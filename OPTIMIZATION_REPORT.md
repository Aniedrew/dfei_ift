# 重建性能优化：剪枝贪心问题与重建方案（收敛版）

> 更新：2026-08-14（v3：新增方案 F「seed-expand 连通性保留剪枝」——node 从硬剪枝幸存节点出发、沿 top-k 边迭代扩展恢复整链；放弃链选择 MLP）
> 主题：为什么信号链会因剪枝丢失、训练与推理的"贪心"差距、收敛方案（edge: top-k / node: seed-expand 方案 F）与文献依据

---

## 0. 训练与评估结果总览（截至 2026-08-14）

### 0.1 当前作业状态（hep_q 检查，08-14 17:10，LZU 停机维护后重启）

| Job ID | 脚本 | 任务 | 状态 | 进度 |
|---|---|---|---|---|
| 9661910 | submit_train_cern_resume.sh | CERN 修复版续训（v32 → 新版本） | 🟢 运行中 | v32 已训至 ep61（best val 36.152），维护中断后从 ep61 继续 |
| — | — | 公开数据修复版 | ✅ 已完成 | v27，早停 @ep21，PerfectReco **38.26%** |

> v32（CERN）在 08-12 停机维护时中断于 ep61（val 35.55 仍在下降，best 36.152 @ep61）。08-14 已用 `train_CERN_normed_resume.yaml`（resume_ckpt 指向 v32 best-epoch=61）重新提交，继续训练。
> v27（公开数据修复版）已于 08-11 正常早停完成，正式评估（thr 0.9 论文口径）见 §0.3。

### 0.2 训练版本历史

| 版本 | 数据 | 权重修复 | 状态 | best val_combined_loss | 备注 |
|---|---|---|---|---|---|
| v8 | CERN 旧数据 `CERN_data_LHCb` | ✗ | 100ep 完成 | 0.923 @ep98 | 旧模型基线 |
| v23 | 公开数据 `converted_LHCbcollision` | ✗ | 75/100 中断 | 0.742 @ep60 | bug 模型 |
| v25 | CERN 新数据 `MC_normed` | ✗ | 100ep 完成 | 0.807 @ep96 | bug 模型 |
| v26 | 公开数据 | ✗ | ep0 即停 | 1.768 | 失败/中断 |
| v27 | 公开数据 | ✅ | ✅ 早停完成（~22ep） | **0.760 @ep21** | **修复版，PerfectReco 38.26%** |
| v28/v29 | 公开数据 | — | 快速失败 | — | 落在坏 GPU（gpu02）上 |
| v30 | 公开数据 | ✅ | ✅ 完成（early-stop @28ep） | 43.2 @ep13 | 权重修复重训，**截断过早** |
| v31 | CERN 新数据 | ✅ | 早停 @~70ep | 36.2 @ep56 | 权重修复重训，class-1 70.6% |
| **v32** | CERN 新数据 | ✅ | 🟢 运行中（维护中断于 ep61，08-14 已重提） | **36.152 @ep61** | 续训 v31（降LR 1e-4），仍在下降 |

### 0.3 评估结果（论文口径）

**公开数据（converted_LHCbcollision，thr 0.9）：**

| 模型 | Perfect | AllParticles(含Perfect) | NoneIso |
|---|---|---|---|
| v23（bug） | 19.76% | 55.25% | 12.56% |
| **v27（修复版）** | **38.26%** | **92.30%** | **7.70%** |

> 权重 bug 修复带来 **+18.5pp** PerfectReco 提升（19.8% → 38.3%），NoneIso 从 12.6% 降到 7.7%，验证了修复假设。这也说明分类质量修复后，剪枝误删已成为下一步主要瓶颈（§6.2 预测"只修 bug → 42-50%"，实际 38.3%，接近区间下限，剩余差距正是剪枝级联丢失）。

**CERN 新数据（MC_normed，thr 0.2 口径）：**

| 模型 | 数据 | Perfect | AllParticles(含Perfect) | NoneIso |
|---|---|---|---|---|
| v25（bug） | CERN 新 | 27.68% | 55.57% | 6.03% |
| v32（修复版） | CERN 新 | 待评估（重训中） | — | — |

### 0.4 关键结论（详见 §6）

1. **bug 模型在 CERN 数据上 per-chain perfect 27.7%**，已高于论文固定权重模型在其数据上的 per-event 21.5%（口径与数据难度不同，仅参考）。
2. **链死亡分解**（201 事件实测）：thr 0.2 下 edge 剪枝杀死 30.5% 的链、node 剪枝杀死 26.3%（高度重叠）；根因是"链存活 = 全节点 AND 全边同时保留"的级联条件。
3. **实测收益**：只修 weight bug → 公开数据 PerfectReco 19.8%→**38.26%**（v27，✅ 已实测）；CERN v32 续训中。
4. **下一步**：方案 F「seed-expand 连通性保留剪枝」——把"链级 AND 存活"换成"链上单节点存活 + top-k 边扩展找回整链"，纯解码、可立即在修复版上验证（§4 F）。

### 0.5 Loss 分解（修复版，2026-08-14，metrics.csv 实测）

`combined = LCA + t_nodes + 33·tt_edges + pv_asso`（各分量实际贡献占比）：

| 版本 | LCA | t_nodes | **tt_edges(×33)** | pv_asso | combined |
|---|---|---|---|---|---|
| v27（公开，修复版） | 0.7% | 17.9% | **81.3%** | ~0% | 0.760 |
| v32（CERN，修复版，ep52-61） | 1.5% | 5.8% | **90.9%** | 1.7% | 36.9 |

**结论**：训练被 **tt_edges（边剪枝）绝对主导**（×33 放大后 81-91%），node 分类仅占 5.8-17.9%、**无任何放大系数**，LCA 分类仅 1.5%（×class 逆频率后仍极小）。这解释了：
- **CERN 上 node 误剪更严重**（v32）：node 任务在 loss 里权重仅 5.8%，模型动力学不足；
- **结构正确率的上限**：LCA 分类（决定链形状）在 loss 中近乎被忽略，即便分类质量已修复，模型仍缺乏动力优化 LCA/结构。

**训练侧再平衡建议**（与方案 F 推理侧互补，零代码量、下次重训生效）：
```
combined = LCA + w_node·t_nodes + 33·tt_edges + w_pv·pv_asso
建议: w_node ≈ 10-20, w_lca ≈ 10-20 (让 node/LCA 得到与 tt_edges 同量级的梯度投入)
另: LCA class weights 在 CERN 数据上出现 inf (y2/y3 为 0), 需 clip (min(weight, ~1e3))
```
详见 §4.4。

---

## 1. 先把"贪心"拆清楚：问题分三个层次

"这个算法贪心吗？" 不能一概而论。整个管线分三段，每一段的"贪心"性质完全不同：

| 阶段 | 是贪心的吗 | 说明 |
|---|---|---|
| **① 训练（多任务学习）** | ❌ 非贪心 | 四个任务的损失同时反向传播、联合优化（§5.3），不存在"逐步决策"。你说的"所有 MLP 同时训练"正确。 |
| **② 推理剪枝** | ✅ 贪心（局部） | 对每条边/每个节点**独立**按阈值砍（`edge_weight > thr`），只看单个置信度，不考虑"砍掉这条边会不会破坏下游某条链"。错误会在这一步**埋下**。 |
| **③ 衰变树重建** | ✅ 贪心（顺序） | `reconstruct_decay` 按 LCA 深度从 1 开始逐层做连通分量聚类：每层只基于当前子图做局部决策，**不回溯**。某一层的错边直接导致链形状错。 |

**结论**：模型训练是全局联合优化（非贪心）；但**推理管线（剪枝→重建）确实是贪心的，且与训练是割裂的**——训练优化的是"分类损失"，推理用的是"硬阈值 + 顺序聚类"。这种"训练目标与推理决策不一致"在 ML 里叫 **train-inference gap**（训练-推理差距），正是信号链丢失的根源。

---

## 2. 为什么硬剪枝会"级联"丢信号（有数据）

在论文测试数据上用 v23 模型做的剪枝流失统计（1000 事件级别，趋势已确认）：

| 阈值 | 信号径迹存活 | 正边（y>0）存活 | **完整链存活** |
|---|---|---|---|
| 0.01（近乎不剪） | 100% | 100% | 100% |
| 0.1 | 100% | 97% | ~82% |
| 0.2（当前/论文） | 98.3% | 93.6% | **~82%** |
| 0.5 | 98.3% | 81.6% | ~64% |

**关键观察**：
1. 节点剪枝几乎不丢信号径迹（98%+），边剪枝丢 6-18% 的正边。
2. **完整链丢失远大于单边丢失**（0.2 时链丢 18% > 正边丢 6%）。原因：一条链要"完整存活"要求**所有节点和所有正边同时保留**——这是 AND 条件，单点错误在链级别被放大（级联效应）。
3. 一旦某条"姐妹边"（LCA 类 1）被剪掉，该链在重建时直接断成两截 → PerfectReco 失败。

**根因排序**（按对重建的影响）：
- ① **LCAG 分类质量**（最主要）：类 1 准确率 0%（权重 bug 所致，已修复）→ 大量真姐妹边置信度低 → 剪枝误删。**权重修复后的重训可能直接缓解大部分问题**。
- ② 硬阈值剪枝本身：即使分类完美，阈值也永远有误删；且阈值是人工定的，不随事件/链状态自适应。
- ③ 重建贪心聚类：无回溯，错误不可修复。

---

## 3. 文献调研：ML 界怎么解决"离散决策 + 贪心"的问题

### 3.1 让离散剪枝"可微"：Gumbel-Softmax 家族

**问题**：剪枝是离散决策（保留/删除），没法反向传播。经典解法是 Gumbel-Softmax（连续松弛）：

- **Jang, Gu, Poole, ICLR 2017**（arXiv:1611.01144）：把"从离散分布采样"替换成加 Gumbel 噪声的 softmax，用温度 τ 控制软硬：τ→0 时退化为硬 one-hot，τ 大时是软概率。梯度可以穿过这个软采样。
- **Maddison et al. 2017**（arXiv:1611.00712）：独立发现，称 Concrete distribution。
- **Bengio et al. 2013**（Straight-Through）：前向用硬决策、反向用软梯度。

**对应到我们的问题**：把"边/节点是否保留"变成一个温度退火的软掩码（σ(weight/τ)），训练时让模型**亲眼看到"剪枝"这个动作**（而不是训练时全图、推理时才剪），学得更鲁棒。这就是你提的"根据概率保存一部分"的可微、端到端版本。

### 3.2 用随机采样代替硬决策：MC/集成思路

- **边 dropout / 随机掩码**：消息传递中的 dropout（Srivastava 2014 的思想）就是在训练时随机丢弃一部分连接，让模型对"缺边"鲁棒。
- **MC 采样式推理（Ensemble）**：推理时按 (1−p) 概率多次随机保留低置信边，多次重建取"一致性最高的链"。本质是**对硬阈值做随机化 + 多数投票**，缓解单次贪心误删。代价是推理成本 ×K 次。
- 这些与 Gumbel-Softmax 的关系：MC 采样是"推理时的随机化"，Gumbel 是"训练时的可微随机化"，两者可结合。

### 3.3 彻底跳出"边分类 + 贪心聚类"：端到端图分割

HEP 社区近年主流趋势是**不经过"分类→剪枝→聚类"的串行贪心管线**，而是单次前向直接输出粒子候选：

- **Object Condensation（Qasim, Kieseler et al., EPJC 2022, arXiv:2204.01681）**：CMS 量热计端到端重建。给每个候选粒子一个"凝聚点（condensation point）"表征，训练损失鼓励同一粒子的 hits 向凝聚点聚集、不同粒子互相排斥。**单次前向完成聚类，无贪心后处理**。
- **GNN tracking（TrackML/ExaTrkX）**：径迹重建的主流做法仍是"边分类 + 图分割"，但分割用**谱聚类/社区发现**这类全局方法，而非逐层贪心。
- **可微聚类 / 最优传输（Sinkhorn）**：把聚类写成可微的最优传输问题，端到端训练（常见于 jet 重建，如 Sinkhorn AE）。

**对我们的启示**：DFEI 的"LCAG 分类 + 逐层连通分量"本质是"边分类 + 贪心分割"。文献上有两条升级路径：(a) 在**现有分类基础上**做软/随机化剪枝（§4 B/C 方案）；(b) 改用**单次凝聚式**目标，把"聚成衰变树"变成模型直接学的东西（§4 D 方案）。

---

## 4. 方案对比（按"计算量 × 工作量"平衡后的收敛结果）

先给结论：**端到端（D）排除、全量软重建（A）放弃、链选择 MLP（G）放弃**；edge 走 **top-k（E）**，node 走 **seed-expand 连通性保留（F，替代原 B 混合）**。node 与 edge 规模差 100 倍（~150 vs ~23K/事件），必须分开处理；且 `reconstruct_decay` 是离散连通分量聚类，连续权重进不去，所以"软"只能体现在**候选集怎么定**这一层。

| 方案 | 适用 | 计算量 | 工作量 | 结论 |
|---|---|---|---|---|
| A 全量软重建 | 边 | 爆炸（全图 ~11K 对 + 误判背景边污染） | 低（纯解码） | ❌ 放弃，仅作极端对照 |
| E top-k + 轻软 | 边 | O(N×k)，硬上限可预算 | 低（纯解码） | ✅ 并入 F 的扩展步骤 |
| B1 推理 MC | 节点 | 仅重建段 ×K | 低（纯解码） | ⭕ 被 F 取代（F 不引入随机性） |
| B2 训练 Gumbel | 节点 | 训练开销≈一次向量化张量乘 | 中（改训练+重训） | ⭕ 保留为 F 之后的训练侧升级 |
| **F seed-expand 连通性保留** | **节点+边** | **O(hop × E)，毫秒级** | **低（纯解码）** | **✅ 首选，立即验证** |
| G 链选择 MLP（第5头） | 重建后 | 训练开销小 | 中（已实现） | ❌ 放弃（GNN 自身已做选择，见下） |
| D 端到端 | 全部 | 高 | 极高（重新开发） | ❌ 暂缓 |

### 方案 A：全量软重建 → 放弃（计算量 + 正确性双输）

- **计算量**：不剪（或阈值极低）→ ~99% 的背景边全部进入候选。全连接图 ~150 径迹 ≈ 11K 无向对（~23K 有向边），而当前 0.2 阈值后只有几百条，差一个数量级；`reconstruct_decay` 对每条剩余边反复做 O(E) 的 proxy-link 查找，E 变大直接拖慢聚类。
- **更关键的是正确性**：`reconstruct_decay` 只做离散聚类（连通分量），根本没有"按权重参与"的接口——所谓"软权重"无处安放。全图进入后，背景对即使只有 1% 被 LCAG 误判成类 1/2/3，也有 ~100 条坏边，远超真姐妹边（每条链只有几条），会把整个事件并成一个大簇，链全错。**所以全量软不只是慢，是错。**
- **结论**：软重建不能"全量"，必须配合候选收缩 → 方案 F（其扩展天然收缩候选）。

### 方案 E（edge）：top-k 候选 + 轻软 → 并入 F

- **top-k 定义候选集（计算量硬上限）**：每条径迹按 `edge_weight` 保留相连的最可信 k 条边（k≈10-20）。全事件候选 ≤ N×k 条有向边（N≈150，k=20 → ≤3K，去重后 ~1.5K 无向对，约为全图 11K 的 13%）。**相比硬阈值（保留多少条取决于置信度分布、无法预算），top-k 给计算量一个确定的账。**
- 文献支撑：K-Neighbor sparsifier（arXiv:2603.06952）与 per-node top-k 同源——"每个顶点至少保留 min(k, d_i) 条边，无向边由任一端点选中即保留"，实测在 GNN 管线上稀疏化"往往保持甚至提升精度"，K-Neighbor 稀疏化在 Products 图上推理加速 11.7× 仅掉 0.7% 精度。
- **本方案 E 的实现**（`topk_selection.topk_edge_selbool`）已含"任一端点选中即双向还原"语义。**F 的扩展步骤天然包含 E**（幸存节点的 top-k 边即 E），故 E 不再单独验证，直接作为 F 的子步骤。

### 方案 F（node+edge 首选）：seed-expand 连通性保留剪枝（用户新方案，2026-08-14）

**核心洞察（为什么它比"阈值+top-k"更优）**：硬剪枝丢链的根因是"链要完整存活 = 所有节点 AND 所有正边同时保留"（§2 的级联效应）。方案 F 把这个 AND 条件换成"**只要链上有一个节点幸存，整条链通过 top-k 边扩展找回**"——连通性由构造保证，不再依赖逐元素巧合。

**算法（f：node → edge → node 交替传播）**：
```
输入: node_weight[N], edge_weight[E], edge_index[2,E]
      k0 (首跳 top-k), thr_seed (种子阈值), max_hop (或 k 递减到 0 自动停)
1. 种子集 S = {n : node_weight[n] > thr_seed}          # 硬剪枝幸存节点
2. hop = 0; k = k0
3. while k > 0 and hop < max_hop:
     # 扩展: 对当前 S 中每个节点的关联边取 top-k (按 edge_weight)
     keep_edges = per-node top-k(S, k)                  # 双向还原
     new_nodes = {边端点 ∉ S 但被 keep_edges 连到}        # 被误剪节点加回
     if new_nodes 为空: break
     S ∪= new_nodes
     hop += 1; k -= 1                                   # 递减: 控制波前扩散范围
4. 最终边集 = keep_edges 累积 (或: keep_edges ∪ {S 内低置信边}) → 进 reconstruct_decay
```

**为什么 k 递减（k-1）**：等价于"最多 k0 跳、第 h 跳的扩展宽度为 k0-h"的受限波前。链条长度有限（B 介子衰变链 ≈ 3-8 个粒子），k0=10、3-4 跳即可覆盖整链；递减防止高度连通事件把大块背景拉进来（雪崩控制）。参数化建议用 `k0` + `max_hop` 而非硬编码"减到 0"。

**与硬阈值的关系**：种子阈值 `thr_seed` 取 node_prune_thr（0.9）或更宽松（0.5）皆可——种子越严，找回依赖扩展越多；建议扫 {0.5, 0.7, 0.9}。扩展跳内 top-k 也可叠加"LCAG 类 >0"过滤（类 0 背景边不进重建）。

**保证的性质**（对应 §2 痛点）：
- 链内任一节点幸存 → 整链节点+边被保留（**连通性恢复**，直接消除 edge 剪枝 30.5% / node 剪枝 26.3% 的级联丢失）；
- 计算量硬上限：每次扩展 ≤ |S|×k 条边，hops ≤ k0，全事件 O(k0·N·k0) ≈ 毫秒级（torch 向量化，无需逐节点 Python 循环）；
- 与现有代码隔离：仅改 `reconstruction.py` 剪枝段 + `topk_selection.py` 新增 `node_expand_selbool`，配置开关 `node_expand`，不污染默认逻辑。

**理论对应（文献）**：这正是图论里的 **seed set expansion / 局部图聚类**——从种子节点出发、运行时间只与输出簇大小成正比（Nibble：Spielman & Teng 2004；PageRank-Nibble：Andersen, Chung & Lang 2006；Heat-Kernel-PageRank：Kloster & Gleich 2014）。我们的"递减 top-k"是其中最简单的截断扩散；更全局的升级是**扩散分数 + sweep cut**（见 §4.2）。

#### 4.1 方案 F 的升级版 F'：扩散分数 + sweep cut（若 F 收益有限）

- **Motivation**：贪心 top-k 只看一跳内最高分边，可能把高置信背景边也拉进链（False Positive），且对"链内低分边"的恢复不敏感。F' 用全局扩散信息替代 myopic 选择：
  1. 从种子集 S 出发，迭代扩散分数 `p = (1-α)·1_S + α·Âᵀp`（一阶 PPR 或 heat-kernel 展开，截断小分数）——**每个节点得到"与种子集的连通强度"**；
  2. 保留分数高于阈值/做 sweep cut 的节点（比 top-k 更全局、有 conductance 保证）；
  3. 边集 = 保留节点间的边（可再按 edge_weight top-k 收缩）。
- **代价**：每事件多一次稀疏矩阵-向量乘（~23K 边，<1ms），工作量低；作为 F 的对照实验，先跑 F 再看是否值得。

#### 4.2 为什么放弃链选择 MLP（方案 G，2026-08-14 新认识）

- **原动机回顾**：多候选重建后需要一个"选链"打分器（`CandidateScorer` 第5头）。**新的认识：这个头是多余的**——GNN 的消息传递已经在训练中学到了"哪些节点/边组合构成真实衰变树"，重建阶段要做的不是"再学一个选择器"，而是**把足够多的候选节点/边喂给重建**（候选集越完整，GNN 学到的结构信息越能体现）。方案 F 正是"喂足够候选"的机制：top-k 扩展保证候选包含完整链，链的好坏由 GNN 的 edge_weight/LCAG 决定。
- **证据链**：v27 修复版（仅修 weight bug，无任何选择器）PerfectReco 19.8%→38.3%，说明结构选择能力来自主干本身；重建候选缺失（级联剪枝）才是瓶颈，而非"选不出链"。
- **处置**：`SELECTION_MLP_DESIGN.md` 第1、2步代码已实现并验证（不删除，可作未来"多候选重排"备选），但**不再作为主线路线**，报告路线改为方案 F。

#### 4.3 模型结构检查（2026-08-14）：MLP 顺序无需调整

**当前结构**（`DFEI_HGNN`，每层 GN block 内部顺序）：

```
encoder → [block1 → ⊕latent → block2 → ⊕latent → block3 → ⊕latent → block4] → decoder → op_trafo
每个 block:
  ① edge_block 更新边特征 (MLP_forward: 128→128→16)
  ② edge 推理: edge_weights = σ(MLP_infer(edges))     ← 在 node 更新之前
  ③ node_block 更新节点特征, 用 edge_weights 加权消息聚合 (weighted_pass)
  ④ node 推理: node_weights = σ(MLP_infer(nodes))     ← 在 node 更新之后
  ⑤ global_block
```

**检查结论**：
1. **①→②→③→④ 顺序自洽**：边权重在节点聚合之前算（attention 式：先判边重要性再加权消息），且边特征已由 edge_block 用上一层信息更新过；节点权重在节点聚合之后算（此时特征含边上下文，信息最全）。**不需要重排**。
2. **node 被剪的根源不是 MLP 顺序**，而是 **train-inference gap**（训练全图分类、推理硬阈值剪枝）——方案 F 正是从推理侧消除这个 gap（不删节点、扩展保留）。
3. **可选的训练侧增强（需重训，不改变顺序）**：
   - 把 node_weights 提前到 ③ 之前计算，并对节点特征做 `node_weight` 门控（让信号节点表征更显著，类似边权重的作用）——列为 B2 之后的候选；
   - B2 Gumbel 可微剪枝（已有文献依据 §3.1）。
4. **dense 残差 concat**（`hetero_graph_concat(latent, data)`）使每层输入维度 16→32→48→64 增长（DenseNet 式），block 数=4 时特征维度偏高；这是论文架构，不动。

**结论**：模型结构保持现状，不做 MLP 重排；推理侧问题用方案 F 解决（零重训成本）。

#### 4.4 训练侧再平衡（loss 分解驱动，2026-08-14）与 LCA GNN 优化

**① Loss 权重再平衡（零代码量，下次重训生效）**
实测 loss 组成（§0.5）：tt_edges ×33 占 81-91%，t_nodes 5.8-17.9%（系数 1），LCA 仅 1.5%，pv 1.7%。node/LCA 分类的梯度投入严重不足——**这正是 CERN 上 node 误剪严重的模型侧原因**（与 §4.3 结论互相印证）。
```
combined = LCA + w_node·t_nodes + 33·tt_edges + w_pv·pv_asso
建议初值: w_node = 10, w_lca = 10 (把 node/LCA 的梯度占比提到 ~20-30% 量级)
```
另需修复：CERN 数据 LCA class 2/3 样本为 0 → `transform_pos_weight` 产出 **inf 权重**（此前观察到的 `LCA=[0.25, 625, 1250, inf]`）→ CrossEntropyLoss 输出 inf → 依赖现有 nan_to_num/clamp 兜底。应在 `weights_calculator.transform_pos_weight` 对 LCA 权重做 **clip(≤1e3)**，消除隐患。

**② 更高效的边/点保留策略（推理侧，方案 F 的升级方向）**
- **F'：扩散分数 + sweep cut**（§4.1）——用一次稀疏矩阵-向量乘（PPR/heat-kernel）替代每跳贪心 top-k：全局连通强度排序 + 边界切割，比贪心更少拉入高分背景边，且有 conductance 理论保证。实现工作量低（scipy/sparse 稀疏乘）。
- **自适应种子**：`seed_thr` 固定阈值对每事件可能过松/过紧；改为**按事件取 node_weight 前 N% 高分节点作种子**（rank-based），鲁棒性更好，无需对 thr 扫参。
- **向量化 top-k**：当前 `_per_node_topk_incident` 是逐边贪心（Python 循环，每跳 ≤150 次），可用 `torch.topk` + 按节点分组（unique/scatter）向量化，O(E log k) 无循环，推理提速。

**③ LCA GNN 优化可能（结构侧）**
- LCA 分类头在 op_trafo（decoder 之后），特征已含 4 层消息传递信息，架构合理；**瓶颈是 loss 占比 1.5%**（①已覆盖）。
- 可选增强：op_trafo 的 tr_tr 头输出 4 类 logits 直接进 `lca_truth_matrix/reconstruct_decay`；若想提升 LCA 结构一致性，可在重建时对 LCA 输出做**温度缩放/argmax 一致性**后处理（低成本），列为 F 之后的候选。
- 结构结论与 §4.3 一致：**不需要改 MLP 顺序**，先做 loss 再平衡 + 方案 F。

### 方案 B（node）：B1 推理 MC + B2 训练 Gumbel 混合

- **规模依据**：node ~150/事件 vs edge ~23K/事件，差 100 倍 → node 承受得起 MC 的 ×K 成本，edge 不行。这正是 node 和 edge 必须分治的原因。
- **B2（训练时，Gumbel-Softmax）**：节点保留/删除做成温度退火的软掩码，模型训练时就"经历"剪枝 → 消除 train-inference gap；同时让 bkg 节点的高分残留（NoneIso 的一大来源）在训练中被压下去。
- **B1（推理时，MC 采样）**：对将被剪的低置信节点按概率保留，K 次重建取跨采样最稳定的链（集成投票）。
- **GPU 并行性（都不是逐节点循环）**：
  - **B2**：Gumbel 噪声采样与掩码施加都是**向量化张量操作**——`u = torch.rand_like(x)` 一次性给整批所有节点采噪声，掩码 `x * soft_mask` 一步完成，天然按 batch 在 GPU 上并行，训练开销≈每次前向多一次张量乘。
  - **B1**：K 个掩码直接作用在**已算好的** `node_weights` 上（纯后处理向量化，不必重跑前向）；若想让掩码影响消息传递，也可把 K 份图复制拼成一个 batch 一次前向，GPU 并行。真正重复的只有 K 次 `reconstruct_decay`（单事件毫秒级，并入已有 ThreadPool(4) 线程并行）。
- **⚠️ 诚实的收益评估**：0.2 阈值下 node 剪枝只丢 ~2% 信号径迹，所以 **B1 的收益上限很低**（最多救回被误剪的那 2%），且 B1 不解决 bkg 节点被保留（False Positive）的问题。**结论：B1 是低成本"先试一下"的项（纯解码）；B2 价值更高，应并入下一次重训。**

### 方案 C：每节点 top-k → 已并入 E

原"每节点 top-k 保边"已并入方案 E；"二次确认（iterative，剪后重评）"需要两次前向，计算量偏高，暂缓。

### 方案 D：端到端可微重建 → 暂缓（=重新开发一个模型）

- 需要重设计损失与标签（LCAG → "簇归属"）、重新训练、重新评估，等于重新开发；且 DFEI 的衰变树层级（类 1/2/3）比普通聚类更精细，直接迁移 Object Condensation 需谨慎。
- 保留为长期方向（文献 §3.3），仅在"top-k + 软重建"的天花板仍不够时再考虑。

---

## 5. 推荐路线（v3 更新：方案 F 为主线）

```
第 0 步（✅ 已完成）：权重修复重训
   CERN: v31/v32（v32 续训中，从 ep61 继续）
   公开: v27 ✅ 完成，PerfectReco 38.26%（+18.5pp）
   结论: 修复假设成立；分类质量已修复，剪枝级联丢失成为下一步主瓶颈。

第 1 步（立即，纯解码）：方案 F「seed-expand 连通性保留剪枝」
   在 v32/v27（修复版）上实现 node_expand_selbool：
   节点硬剪枝(种子) → per-node top-k 边扩展 → 加回被误剪节点 → k 递减迭代。
   扫参: thr_seed ∈ {0.5, 0.7, 0.9} × k0 ∈ {8, 12, 16} × max_hop ∈ {3, 4}
   对比: 硬阈值 0.9/0.2 vs F —— PerfectReco / per-chain 指标 / 候选边数。
   预期: 链存活 69.5% → ~90%+（连通性由构造保证），救回大部分剪枝级联丢失。

第 2 步（视第 1 步收益）：方案 F'「扩散分数 + sweep cut」对照
   若 F 中 top-k 拉入过多背景（NoneIso 回升）→ 用 PPR/heat-kernel 扩散分数
   替代贪心 top-k，sweep cut 定边界。

第 3 步（若需要，训练侧）：node B2（Gumbel-Softmax 训练）
   可微剪枝并入下一次重训，消除 train-inference gap；与 F 互补（F 是推理侧）。

第 4 步（长期，暂缓）：方案 D（Object Condensation 式端到端重建）
   工作量≈重新开发模型，仅在 F/F' 的天花板仍不够时重启。
```

**优先级判断依据**（计算量 × 工作量平衡）：
- 方案 F 是纯解码改动（只改 `reconstruction.py` 剪枝段），成本最低，立刻能拿到"候选机制 vs 分类质量"的定量答案。
- 链选择 MLP（方案 G）已放弃：GNN 主干自身已学得结构选择，只需喂足够候选（§4.2）。
- B2/D 需重训或重开发，排后；端到端（D）当前阶段排除。

---

## 6. 预期收益估算（v27 公开已实测；CERN 基于分解 + 论文 Table1/Table2）

### 6.1 失败分解（v27 公开修复版 + bug 模型分解，thr 0.9/0.2）

| 来源 | 占比 | 说明 |
|---|---|---|
| 完美重建（v27 公开，修复版） | **38.26%** | bug 版 19.76% → +18.5pp |
| 剪枝级联杀死 | ~20-30% | 修复版链存活率提升但级联丢失仍在（AND 条件） |
| 存活但结构错/找不到 | 其余 | 修复版分类质量已显著改善 |

> 链死亡分解（bug 模型，thr 0.2）：edge 剪枝杀 30.5%、node 剪枝杀 26.3%（高度重叠）。**方案 F 的连通性恢复直接把"链级 AND 存活"变成"单节点存活"**，可消除绝大部分此类丢失。

### 6.2 机制化估计（Perfect ≈ 链存活 × 结构正确率）

| 方案 | 链存活 | 结构正确率(给定存活) | 预计 Perfect |
|---|---|---|---|
| bug（实测 v23/v25） | 69.5% | ~40% | **19.8-27.7%（实测）** |
| 只修 weight bug（实测 v27） | ~80% | ~48% | **38.26%（实测，公开）** |
| 修 bug + 方案 F（推理侧） | ~90-95% | ~48-55% | **~45-52%** |
| 修 bug + F + B2（训练侧） | ~95% | ~55-65% | **~52-62%** |

### 6.3 估算依据与假设（务必阅读）

- **v27 公开实测 38.26%** 已高于"只修 bug ~42-50%"估计区间的下限——分类质量修复后，剪枝级联丢失的占比相对上升，**方案 F 的边际收益（推理侧、零训练成本）预计在 +5-10pp**。
- **CERN 侧**：v32 还在训练，其评估（thr 0.2）未出；F 的效果以公开数据先验证，CERN 复验。
- **⚠️ 三个不确定性**：
  1. F 的扩展会引入更多候选边 → NoneIso 可能回升（背景边被拉入链），需在"链存活 ↑ vs NoneIso ↑"间调 thr_seed/k0；
  2. CERN 数据几乎没有 y2/3 样本，结论主要靠 y1=76% 与公开数据；
  3. F 的扫参未做，收益按"连通性恢复上限"估计。
- 若换算成 **per-event**（per-chain/per-event ≈ 1.28）：修 bug + F ≈ 35-41%，修 bug + F + B2 ≈ 41-48%。

---

## 7. 参考文献

| 论文 | 方法 | 与本文问题的关系 |
|---|---|---|
| Spielman & Teng 2004 (Nibble) | 局部图分割：从种子出发、时间与输出簇成正比 | 方案 F 的理论源头（seed set expansion） |
| Andersen, Chung & Lang, FOCS 2006 (arXiv:cs/0603077) | PageRank-Nibble：个性化 PageRank 局部分割 + sweep cut | 方案 F' 的扩散分数 + 边界切割依据 |
| Kloster & Gleich 2014 (HeatKernel-PageRank) | 确定性 heat-kernel 扩散局部聚类 | F' 的另一扩散算子（优于 PPR 的实证） |
| Song et al. 2026 (arXiv:2603.06952) | GNN 管线图稀疏化：K-Neighbor / Rank-Degree / Local-Degree | 验证 per-node top-k 边保留（方案 E/F 的边扩展）有效且低损 |
| Allen Zhu et al., ICML 2013 | 局部算法找 well-connected cluster（内部连通性） | 支持"保连通性优先于单点置信" |
| Jang, Gu, Poole, ICLR 2017 (arXiv:1611.01144) | Gumbel-Softmax：离散变量可微采样 | 可微剪枝掩码的理论基础（B2 保留项） |
| Qasim, Kieseler et al., EPJC 2022 (arXiv:2204.01681) | Object Condensation 端到端图分割 | 消除"分类+贪心聚类"的参考范式 |
| TrackML / ExaTrkX | GNN 径迹重建（边分类 + 图分割） | 边分类管线的工业级实践 |
| arXiv:2504.21844（本项目论文） | DFEI：LCAG 多任务 + tight pruning | 本文方法出处，剪枝阈值 0.2 |

> 附：本报告对应代码位置——剪枝：`wmpgnn/util/pruners.py` + `wmpgnn/reconstruction/reconstruction.py`；重建聚类：`wmpgnn/reconstruction/reco_helper.py::reconstruct_decay`；剪枝流失统计：`analyze_pruning_loss.py`；方案 E 实现：`wmpgnn/reconstruction/topk_selection.py::topk_edge_selbool`（F 将在此新增 `node_expand_selbool`）。
