# 链中心性过滤器：用 Rumor Centrality 剔除噪声链

> 更新：2026-08-14（v2：新增链级 LCA 物理置信度判据——"最物理"而非"最像树"）
> 灵感来源：传染病溯源（Epidemic Source Detection）的 **Rumor Centrality**（Shah & Zaman, 2011）。
> 代码：`wmpgnn/reconstruction/topk_selection.py`（`chain_center_score` / `filter_chains_by_center` / `chain_lca_score` / `filter_chains_by_lca`）
> 接入：`wmpgnn/reconstruction/reconstruction.py::reconstruct_single_evt`

---

## 1. 一句话

- **结构判据**（§3-4）：真实衰变链是有清晰根（B 介子）的树；噪声团块是没根的乱麻。用 rumor centrality + 树状程度剔除乱麻。
- **物理判据**（§5-7，**推荐**）：真链的链内边是模型**高置信**的物理关系；噪声链是模型勉强/误判成的。用链内边平均 LCA 置信度剔除噪声。

> 核心原则：图论结构只是物理规律的**弱代理**。能直接用物理量（LCA 关系置信度）时，就不要退而求其次用结构近似。

---

## 2. 为什么要做这个（问题背景）

重建阶段（`reconstruct_decay`）会输出多条候选链，其中混有**噪声链**（背景粒子被错误连成团块）。它们造成 **NoneIso**（混入无关粒子）——v27 公开修复版的 NoneIso 是 7.7%，是 PerfectReco 之外最大的损失来源。

如何区分"真链"和"噪声团"？**结构本身就是信号**：

```
真链（B → D* → D → Kπ）           噪声团（随机背景互连）
      B                                A──B
     /|\                              /|\ /|\
    D* D K                           C─D E─F
   /|\                                (处处是环, 没有根)
  D  π ...
  (一棵树, 根=B介子)
```

---

## 3. 核心技术：Rumor Centrality（谣言中心性）

### 3.1 来源与直觉

Shah & Zaman 2011 提出：在传染病/谣言传播图上，给定"感染者集合"，**最有可能是源头**的节点 = 使"以它为根的有根传播树"最自然的节点。数学上就是 rumor centrality：

```
R(v, T) = N! · Π_u (1 / τ_v(u))

τ_v(u) = 以 v 为根时, 子树 u 的大小
```

直觉：如果 v 真是源头，那么从 v 出发的"传播过程"应该是最顺畅的——每个子树都"自然"。这个量对**真正的根**最大。

### 3.2 为什么它适合判"链的真假"

把"传播过程"倒过来理解：
- **真链**：有一个天然源头（B 介子），以它为根的树最"自然" → 它的 rumor centrality 显著高于其他节点 → **中心清晰**。
- **噪声团**：没有天然源头，任何节点当"根"都一样别扭 → 各节点 rumor centrality 差距小 → **中心模糊**。

### 3.3 实现（log 空间，避免数值溢出）

比较 R(v) 只需比较 `log R(v) = -Σ_u log(τ_v(u))`（N! 是常数可省）：

```
对链内每个节点 v:
  以 v 为根做 DFS，算所有子树大小 τ_v(u)
  logR(v) = -Σ log(τ_v(u))
中心 = argmax logR
清晰度 = max logR - 次大 logR
```

### 3.4 一个问题：链不是树怎么办？

重建链（连通分量）可能**有环**（噪声团的特征）。Rumor centrality 只在树上有理论保证，所以：

1. 先在链内取**最大生成树**（按边权重，Kruskal）——把环"最合理地拆掉"；
2. 在生成树上算 rumor centrality；
3. **记录"拆掉了多少边"**：`tree_frac = 生成树边数 / 链内无向边数`。

**tree_frac 是关键补充**：
- 纯树（真链）：`tree_frac = 1.0`（没有环，没拆任何边）；
- 有环团块（噪声）：`tree_frac < 1.0`（环越多越小）。

这两个指标互补，联合过滤：

```
保留条件: 清晰度 ≥ clarity_thr  且  tree_frac ≥ tree_thr
```

---

## 4. 验证（真实结构测试）

| 结构 | 中心 | 清晰度 | tree_frac | 判定 |
|---|---|---|---|---|
| 链形 0-1-2-3-4 | 2（中间） | 0.41 | 1.00 | ✅ 保留 |
| 星形 0 连 1-4 | 0（根） | 1.39 | 1.00 | ✅ 保留 |
| 完全图（5 节点噪声团） | 任意 | 1.39 | **0.40** | ❌ 剔除 |

- 星形中心最清晰（clarity 1.39）——正是 B 介子衰变的结构；
- 完全图虽然 MST 后也有"伪中心"，但 **tree_frac=0.40 暴露了它是一团乱麻** → 被剔除。

---

## 5. 配置与参数

```yaml
inference:
  chain_center_filter: true       # 开启链中心性过滤
  chain_center_clarity: 0.3       # 中心清晰度阈值 (max-次大 logR)
  chain_center_tree_thr: 0.7      # 树状程度阈值 (MST边/链内边)
```

| 参数 | 默认 | 含义 | 调参方向 |
|---|---|---|---|
| `chain_center_clarity` | 0.3 | 中心越唯一越安全 | NoneIso 高 → 调大；PerfectReco 降太多 → 调小 |
| `chain_center_tree_thr` | 0.7 | 链越像树越安全 | 同上 |

> 建议先跑 `clarity=0.3, tree=0.7`，看 NoneIso 是否下降、PerfectReco 是否保持；若 PerfectReco 受损（真链被误杀），说明阈值过严，放宽。

---

## 6. 与其他方案的关系

```
方案F   (seed-expand top-k)   : 决定"喂哪些点/边进重建"（候选完整性）
方案F'  (扩散+sweep cut)      : 同上, 更全局 (压 NoneIso)
链中心过滤 (rumor centrality) : 重建**之后**过滤 (再压 NoneIso, 结构判据)
链级LCA判据 (物理置信度)      : 重建**之后**过滤 (再压 NoneIso, 物理判据, 推荐)
链选择MLP (已放弃)            : 之前想用学习的打分器选链 —— 现在用结构/物理先验零参数替代
```

三者互补且都**零训练成本**（纯推理侧）。链路：**F/F' 保证真链进得来 → 链级判据保证噪声链出不去**。

---

## 7. 链级 LCA 物理置信度判据（"最物理"，推荐）

> 用户 2026-08-14 提出：我们真正该关心的是"**最物理**"，而不是"最像树"。树状/中心性只是物理的弱代理。

### 7.1 物理直觉

衰变链的本质是**粒子物理关系**（LCAG 分类就是为描述这些关系训练的）：

| LCA 类 | 物理含义 |
|---|---|
| 0 | 无关联（背景） |
| 1 | **姐妹**：同一母粒子的两个 daughter |
| 2 | **同 B**：同一 B 介子产生的粒子 |
| 3 | **两 B**：分属两个不同 B |

一条真 B 链，其链内边**应该全部是模型高置信的类 1/2/3 关系**。噪声链（背景粒子误连成团）的链内边，是模型"勉强判成非背景"的低置信关系。

**判据 = 链内边被判类别的平均 softmax 置信度**：
- 真链：高（~0.9，模型很肯定这些是物理关系）
- 噪声链：低（~0.3-0.5，模型在瞎猜）

### 7.2 与其他功能的**不重合性**（逐项确认）

| 现有功能 | 作用 | 与本判据的关系 |
|---|---|---|
| `reconstruct_decay` | 用 LCA>0 的边**生成**链 | 它决定"哪些边进链"，本判据检查"**已进链**的边是否物理自洽"（生成 vs 校验，不重合） |
| 方案 F/F'（`node_expand*`） | 用 **edge_weight** 选候选边 | 用边剪枝头权重；本判据用 **LCA 分类头**的 softmax 置信度（不同信号源） |
| `chain_center_score` | 用 **结构**（rumor centrality）判链 | 本判据用 **物理**（LCA 置信度）判链（不同维度，可叠加） |
| 链选择 MLP（已弃用） | 学习式链打分 | 本判据是零参数物理先验 |

**结论：不重合。** 唯一需要留意：`reconstruct_decay` 已过滤 `LCA_dec > 0` 的边，所以"链内非背景边占比"这个朴素判据在推理时**饱和**（全为 1）——必须用**置信度**（softmax 概率）而非**类别**（argmax）。

### 7.3 为什么"非背景占比"判据会失效（验证发现）

用 truth 类别（y）验证，朴素判据区分度惊人，但推理时不可用：

| 判据 | truth 链 | 随机假链 | 推理时可用性 |
|---|---|---|---|
| 链内非背景边占比 | 100% | 0.3% | ❌ 饱和：reconstruct_decay 已过滤背景边 |
| 链内类3（两B）占比 | 0% | 0% | ❌ 类3 边数据中本就稀有 |
| 链内类2/3 占比 | 42% | — | ⚠️ 会误杀 31% 真链（部分重建链无母子边） |
| **链内边 LCA softmax 置信度** | 高 | 低 | ✅ **推荐**（不饱和、不误杀） |

### 7.4 实现（`chain_lca_score` / `filter_chains_by_lca`）

```
对每条重建链:
  ① 链内边 = 两端都在链内节点集合中的边 (向量化 torch.isin)
  ② 每条的 LCA softmax 概率 = softmax(graph[tt].lca[链内边])   ← 模型 LCA 分类头输出
  ③ conf = 链内边"被判类别"概率的平均值                        ← 物理置信度
  ④ class2_frac = 链内类2/3 边占比                              ← 辅助 (完整B链更高)
  过滤: conf < conf_thr (或 class2_frac < class2_thr) → 剔除
```

数据来源：`graph[('tracks','to','tracks')].lca`（模型 LCA logits，剪枝后自动对齐保留边，无需额外挂载）。

### 7.5 配置与参数

```yaml
inference:
  chain_lca_filter: true          # 开启链级 LCA 物理置信度过滤
  chain_lca_conf_thr: 0.6         # 链内边平均 LCA 置信度阈值
  chain_lca_class2_thr: 0         # 类2/3 边占比下限 (0=不启用)
```

| 参数 | 默认 | 含义 | 调参方向 |
|---|---|---|---|
| `chain_lca_conf_thr` | 0.6 | 链内边平均置信度下限 | NoneIso 高 → 调大；PerfectReco 降 → 调小 |
| `chain_lca_class2_thr` | 0（关） | 链内类2/3 边占比下限 | 想要"只留完整 B 链"时开（如 0.1），会误杀部分链 |

### 7.6 建议组合

```
链级过滤总开关（推荐配置）:
  chain_lca_filter: true      # 物理判据 (主)
  chain_lca_conf_thr: 0.6
  chain_center_filter: false  # 结构判据 (备选, 若物理判据不够再加)
```

**先只开物理判据**（conf_thr 扫 0.5/0.6/0.7），看 NoneIso/PerfectReco 变化；结构判据作为第二道过滤按需叠加。

---

## 8. 物理意义

- **链的根 = B 介子**：真实 B 衰变链从 B 介子分叉展开，天然是"以 B 为根的树"。rumor centrality 找到的"中心"在物理上就是最可能的 B 介子候选——顺带可以作为"源头识别"的副产品（对应传染病溯源的 Patient Zero 定位）。
- **tree_frac = 链的"树状纯度"**：粒子物理中，同一衰变树内的粒子关系是严格树状的（一个母亲最多两个女儿），不允许环。环的存在 = 混入了不相关的背景关联。

---

## 8. 参考文献

| 文献 | 方法 | 借鉴 |
|---|---|---|
| Shah & Zaman, 2010/2011（arXiv:1110.4522） | Rumor Centrality：树上源估计 MLE | 链中心 = 根(B介子)的自然估计 |
| Tan & Yu, 2023（Foundations & Trends） | 传染源检测数学专著 | 中心性家族全景与理论底座 |
