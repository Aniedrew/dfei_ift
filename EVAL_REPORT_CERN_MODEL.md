# CERN MC 数据训练模型评估报告

## 模型信息

| 项目 | 值 |
|------|-----|
| **模型版本** | version_8 |
| **训练数据** | CERN MC 数据 (CERN_data_LHCb) |
| **最佳 Checkpoint** | epoch=93, val_combined_loss=0.923 |
| **评估所用 Checkpoint** | epoch=98, val_combined_loss=0.923 |
| **测试数据** | CERN MC 测试数据 (10个 tst 文件, ~2000 events) |
| **评估时间** | 2026-07-23 13:26 ~ 13:33 (约 5.5 分钟) |

---

## 评估方法说明

> 本节面向不熟悉图神经网络和粒子物理评估的读者，详细解释报告中各项指标的含义与计算方法。

### 1. 数据与训练设置

#### 数据格式

每个粒子对撞事件被表示为一个**异构图（Heterogeneous Graph）**：

```
图中包含两类"节点"（Nodes）：
  - tracks (径迹): 粒子穿过探测器留下的轨迹，每个事件约50-150条
  - pvs (顶点): 粒子对撞发生的位置，每个事件2-10个

以及两类"边"（Edges）：
  - tracks→tracks: 任意两条径迹之间的可能关联
  - tracks→pvs: 每条径迹与每个顶点的关联

此外还有"全局特征"（globals），描述整个事件的整体信息
```

**数据划分**：训练集每个文件800个事件，验证/测试集每个文件200个事件。评估时使用10个测试文件，共约2000个事件。

#### 训练/验证/测试的划分

- **训练集**：50个 `trn_data_*` 文件，用于模型学习
- **验证集**：5个 `val_data_*` 文件，用于调整超参数和早停
- **测试集**：40个 `tst_data_*` 文件（本报告使用其中10个），仅用于最终评估

#### 数据预处理的步骤

加载数据时，每个文件会依次执行以下操作：
1. **解压**：zstd 压缩的 `.zst` 文件解压
2. **双向化**：将径迹-径迹边翻倍，形成双向图（每条边 `i→j` 复制为 `j→i`），使GNN能从两个方向接收信息
3. **PV关联**：如果配置了 PV 关联模型，用预训练模型做初步关联
4. **校准**：如果启用，对 PID 特征进行标准化处理

#### 分块加载（Chunk Loading）

由于数据集较大（数万事件），采用分块加载策略：将所有训练文件分成若干块（chunk），每块包含2-3个文件（~1600个事件），每次只加载一个块到内存，训练完后释放，再加载下一个块。这样可以控制CPU内存占用。

### 2. 训练损失是如何计算的

模型同时学习**四个任务**，每个任务都有独立的损失函数：

#### 任务1：LCA分类（径迹-径迹关联）

```
问题：任意两条径迹是否来自同一个母粒子的衰变？如果是，有多近的"亲缘关系"？
输出：4个类别（0=无关联, 1/2/3=不同世代的共同祖先关系）
损失：交叉熵损失（CrossEntropyLoss）
```

**交叉熵损失**是一种衡量模型预测的概率分布与真实标签之间差异的指标。值越小，预测越准确。如果模型有100%的把握预测正确类别，损失为0。

#### 任务2：节点剪枝（径迹筛选）

```
问题：这条径迹是来自B强子衰变的"信号"，还是与本底噪声？
输出：0或1（0=信号, 1=本底）
损失：二元交叉熵损失（BCEWithLogitsLoss）
```

#### 任务3：边剪枝（径迹关联筛选）

```
问题：这两条径迹之间真的有物理关联吗？
输出：0或1（0=无关联, 1=有关联）
损失：二元交叉熵损失 × 33（高权重）
```

这个损失的权重被放大33倍，意味着模型的注意力会更多地集中在正确判断径迹间的关系上。

#### 任务4：PV关联（径迹-顶点关联）

```
问题：这条径迹属于哪一个对撞顶点？
输出：每条径迹对每个顶点的归属概率
损失：二元交叉熵损失
```

#### 组合损失（Combined Loss）

四个损失直接相加，作为优化的最终目标：

```
组合损失 = LCA损失 + 节点剪枝损失 + 33×边剪枝损失 + PV关联损失
```

训练时，优化器（Adam）的目标就是最小化这个组合损失。

### 3. 训练过程控制

#### 优化器（Adam）

一种自适应学习率的优化算法。可以把学习率理解为"每次调整模型的步长"：
- 初始学习率：`0.001`（步长较大，快速接近最优解）
- 权重衰减：`0.00001`（L2正则化，防止过拟合）

#### 学习率调度（ReduceLROnPlateau）

```
监视"验证集组合损失"
如果连续5个epoch验证损失没有下降：
  学习率减半（×0.5）
最低学习率：0.000001（不能再低）
```

这意味着：当模型学习变慢时，自动缩小步长，做更精细的调整。

#### 早停（Early Stopping）

```
如果连续15个epoch验证损失没有创新低：
  停止训练（防止过拟合）
```

#### 梯度裁剪（Gradient Clipping）

将梯度值限制在 `[-1.0, 1.0]` 范围内，防止梯度爆炸导致训练不稳定。

### 4. 事件重建是如何工作的

模型训练完成后，评估时会执行完整的**物理事件重建**：

```
步骤1：模型对每个对撞事件输出预测结果
  ├─ 每条径迹-径迹边: 4类LCA概率分布
  ├─ 每条径迹: 是否属于信号
  ├─ 每条径迹-径迹边: 是否是真实关联
  └─ 每条径迹-顶点: 属于哪个顶点

步骤2：根据预测结果重建物理对象
  ├─ 用LCA概率做聚类（哪些径迹属于同一个母粒子）
  ├─ 用边权重筛选真实关联
  └─ 用PV关联将径迹分配到顶点

步骤3：将重建结果与真实标签对比
  └─ 判断重建是否成功
```

### 5. 重建效率的五级分类体系

对每一个真值 B 强子，重建结果被分为以下五个互斥类别：

| 类别 | 含义 | 判断标准 |
|------|------|---------|
| **PerfectReco** (完美重建) | 所有粒子都被正确重建，且衰变层次结构完全正确 | 节点匹配 + LCA值完全一致 |
| **AllParticles** (全面重建) | 所有应该被重建的粒子都被找到了 | 节点键完全匹配（可能LCA略有偏差） |
| **NoneIso** (未隔离) | 所有粒子都被重建了，但混入了额外本底 | 真值粒子全在重建簇中，但簇中还多出了额外的粒子 |
| **PartReco** (部分重建) | 只有部分信号粒子被成功重建 | 20%-100%的粒子被重建到同一簇中 |
| **NotFound** (未找到) | 完全无法重建 | 以上均不满足 |

**判断的具体方法**：
1. 模型输出LCA分类结果，构建邻接矩阵
2. 对邻接矩阵做连通分量分析，形成径迹簇（聚类）
3. 每个径迹簇尝试匹配一个 B 强子真值
4. 对于每个成功匹配的簇，计算真值粒子的覆盖率

### 6. PV关联准确率的度量

**PV关联**：判断每条径迹属于哪一个对撞顶点（PV）。

有两种方法做对比：
- **HGNN方法**：用神经网络预测的径迹-顶点关联概率
- **minIP基线**：传统方法，选择"最小碰撞参数"对应的顶点

**误关联率（Miss-association rate）**：
```
误关联率 = (错误分配到其它顶点的径迹数 / 总径迹数) × 100%
```
**数值越低越好**。HGNN误关联率~1%意味着100条径迹中只有1条被分错顶点。

这个指标还会按不同信号类型细分：
- **所有径迹**：全部径迹的统计
- **信号径迹**：仅来自B强子的径迹
- **B系统**：属于同一个B衰变链的径迹组

### 7. 统计误差的计算

所有百分比报告"均值 ± 统计误差"，基于二项分布的标准差公式：

```
误差 = 效率 × √(1/成功数 + 1/总数)
```

这意味着：如果评估的事件越多，统计误差越小。7083个B强子的统计误差在±0.5-1%左右，精度合理。

### 8. 训练损失与重建效率的关系

```
训练损失（取值范围 0~+∞）         重建效率（取值范围 0~100%）
    ↓ 反映模型预测的"自信程度"          ↑ 反映物理重建的"实际效果"
    
通过组合损失来训练模型，但最终关心的是重建效率。
低损失 不一定保证 高重建效率，
但高损失 通常意味着 重建效率不会好。
```

报告中同时报告这两类指标，因为它们是互补的：损失反映数值优化进展，重建效率反映物理性能。

---

## 1. 重建效率 (Reconstruction Efficiency)

### 1.1 总体重建效率

| 类别 | 效率 | 说明 |
|------|------|------|
| **AllParticles** (总重建) | **35.24% ± 0.82%** | 所有B强子的重建率 |
| **PerfectReco** (完美重建) | **18.66% ± 0.56%** | 完整无误的重建 |
| **PartReco** (部分重建) | **17.46% ± 0.54%** | 部分重建成功 |
| **NoneIso** (未重建) | **0.97% ± 0.12%** | 完全无法重建 |

**解释**：在被评估的 7083 个 B 强子中：
- 约 **35%** 被成功重建（至少部分重建）
- 约 **19%** 被完美重建（所有衰变产物正确关联）
- 约 **1%** 完全无法从孤立径迹中重建

### 1.2 按事件中 B 强子数量分

| 事件类型 | 事件数 | 总重建 | 完美重建 | 部分重建 | 未重建 |
|---------|-------|--------|---------|---------|-------|
| **1个B** | 3,177 | 36.64% | 20.52% | 17.19% | 0.72% |
| **2个B** | 3,744 | 34.46% | 17.39% | 17.87% | 1.18% |
| **>2个B** | 162 | 25.93% | 11.73% | 13.58% | 1.23% |

**趋势**：事件中 B 强子越多，重建效率越低（径迹更拥挤）。

### 1.3 按电荷符号分

| 类型 | B数 | 总重建 | 完美重建 |
|------|-----|--------|---------|
| **负电荷 (b → B⁻)** | 3,541 | 34.76% | 18.02% |
| **正电荷 (b → B⁺)** | 3,537 | 35.76% | 19.34% |

**结论**：正负电荷 B 强子的重建效率基本一致，模型没有电荷偏好。

---

## 2. PV 关联性能 (Vertex Association)

### 2.1 全部径迹

| 方法 | 误关联率 | 说明 |
|------|---------|------|
| **HGNN (神经网络)** | **0.96% ± 0.22%** | 模型预测 |
| **minIP (基线)** | **11.32% ± 0.21%** | 基于最小撞击参数的传统方法 |

### 2.2 信号径迹（按重建质量分）

| 信号类型 | HGNN 误关联率 | minIP 误关联率 |
|---------|-------------|---------------|
| 无选择 (全部信号) | 0.75% ± 1.27% | 29.54% ± 0.99% |
| 完美重建 | 0.53% ± 2.42% | 26.64% ± 1.94% |
| 所有重建粒子 | 0.52% ± 1.55% | 29.42% ± 1.21% |
| 未隔离 | 3.09% ± 8.10% | 32.99% ± 6.20% |
| 部分重建 | 1.07% ± 2.29% | 29.54% ± 1.79% |
| **B 系统** | **0.56% ± 2.82%** | - |

**关键发现**：HGNN 的 PV 关联误判率（~0.5-1%）远低于传统 minIP 方法（~11-30%），**提升了一个数量级**。这是模型最显著的优势。

---

## 3. 训练损失曲线

模型训练了 100 个 epoch，验证集联合损失从 ~2.78 降至 ~0.92：

| Epoch | 训练损失 | 验证损失 | 学习率 |
|-------|---------|---------|--------|
| 0 | 2.777 | 2.777 | 1e-3 |
| 10 | 1.034 | 1.035 | 1e-3 |
| 20 | 1.029 | 1.021 | 1e-3 |
| **28** (LR减半) | 0.979 | **0.979** | 5e-4 |
| 50 | 0.954 | 0.952 | 1.25e-4 |
| 70 | 0.936 | 0.936 | 6.25e-5 |
| **93 (最佳)** | **0.923** | **0.923** | 1.56e-5 |
| 99 | 0.924 | 0.924 | 1.56e-5 |

损失曲线稳定下降，模型收敛良好，没有明显过拟合。

---

## 4. 分类准确率 (LCAG)

LCA 任务（径迹-径迹关联4分类）的各类准确率在训练过程中的表现：

- 训练集准确率始终接近 **99.99%**
- 验证集各类准确率在 **30-55%** 之间（复杂类别略低）
- 类别不平衡显著（大部分径迹对属于"无关联"类）

---

## 5. 生成的可视化结果

评估过程中生成了以下可视化文件，位于 `LHCb_logs/DFEI/version_8/`：

### 5.1 边权重分布与ROC曲线 (4个GN块)

```
plots_00342442_inclusive/edges/
  ├── NN_edges_{0-3}_decision.pdf/png  ← 信号 vs 本底边权重分布
  └── NN_edges_{0-3}_roc.pdf/png       ← ROC曲线
```

### 5.2 节点权重分布与ROC曲线 (4个GN块)

```
plots_00342442_inclusive/nodes/
  ├── NN_nodes_{0-3}_decision.pdf/png
  └── NN_nodes_{0-3}_roc.pdf/png
```

### 5.3 PV关联性能图

```
plots_00342442_inclusive/pv/
  ├── all_tracks_pv_asso.pdf/png          ← 全部径迹PV关联
  ├── NN_pv_asso_{0-3}_decision.pdf/png   ← 各GN块PV关联决策
  ├── NN_pv_asso_{0-3}_roc.pdf/png        ← 各GN块PV关联ROC
  ├── signal_*_pv_asso.pdf/png            ← 不同信号类型的PV关联
  └── signal_b_system_pv_asso.pdf/png     ← B系统的PV关联
```

### 5.4 重建结果CSV

```
  ├── signal_reco_df_00342442_inclusive.csv  ← 7083个B强子重建结果
  └── event_reco_df_00342442_inclusive.csv   ← 事件级重建摘要
```

---

## 6. 总结与评价

### 优势
1. **PV关联极大提升**：HGNN 的 PV 误关联率（~1%）比基线（~11%）提升约 **10倍**
2. **训练收敛良好**：损失从 2.78 降至 0.92，学习率调度正常
3. **无电荷偏好**：正负 B 强子重建效率一致

### 不足
1. **总重建效率偏低（35%）**：仅约 1/3 的 B 强子被成功重建
2. **完美重建率低（19%）**：不到 1/5 的事件被完整重建
3. **多B事件表现下降**：>2个B的事件效率降至 26%

### 与公开数据对比预期

CERN 数据每条事件约 **84条径迹、4200条边**，而公开数据约 **150条径迹、23000条边**。公开数据的 GNN 有更丰富的邻域信息，预期训练出的模型重建效率可能更高。目前公开数据训练作业（batch_size=1, gacc=16）正在排队等待 GPU 资源。
