# DFEI：用图神经网络重建 LHCb 粒子衰变

> 代码：`https://github.com/willsutcliffe/scalable_mtl_hgnn/tree/yukai_IFT`（当前工作分支）
> 论文：arXiv:2504.21844, *Scalable Multi-Task Learning for Particle Collision Event Reconstruction with HGNNs*

## 一页速览

在 LHCb 对撞机实验中，质子-质子碰撞会成对产生 b 夸克（b/反 b），它们各自强子化为一个 B 强子，飞行一段距离后衰变成若干个"末态带电粒子"。探测器只记录这些末态粒子的径迹（动量、位置等信息），**但不知道哪些径迹属于同一个 B 强子**。

**DFEI（Deep Full Event Interpretation，深度全事件解释）** 的任务就是：只看径迹，自动判断径迹之间的亲缘关系——哪些径迹是"姐妹"（同一个母粒子衰变来的）、哪些属于同一个 B 强子、哪些只是无关联的背景。判断出这些关系，就能把每条 B 强子的完整衰变链重新拼出来（重建）。

**方法**：把每个事件看成一张"图"（径迹=节点，径迹对=边），训练一个异构图神经网络（Heterogeneous GNN）对每一条边做 4 分类（LCAG）。训练用的"标准答案"（监督信号）来自蒙特卡洛模拟（MC truth）——模拟器知道每个粒子的真实来源，我们把"哪些径迹对是姐妹"作为标签教给模型。这条主线可以概括为 **truth（真值标签）→ topology（图结构）→ flag（配置开关）→ supervised（监督学习）**。

一句话：**用模拟真值标注的图数据，训练一个多任务 GNN，让它学会从径迹重建 B 强子衰变链。**

---

## 1. 物理背景：为什么这件事难做

### 1.1 一个 LHCb 事件里有什么

一次质子-质子对撞（bunch crossing）中平均约 5 次 pp 碰撞同时发生（Run 3 条件），产生约 150 条带电径迹。其中只有一小部分来自 b 强子衰变（信号），大部分来自碎裂（fragmentation）、底层事件（underlying event）等（背景）。约 40% 的事件包含不止一个 b 强子。

### 1.2 衰变链与"公共祖先"

每条 b 强子衰变形成一棵"树"：B 强子 →（可经多个中间态）→ 末态粒子。两个末态粒子之间的关系可以用"最低公共祖先"（Lowest Common Ancestor, LCA）描述：

| LCAG 类 | 含义 | 例子 |
|---|---|---|
| 0 背景 | 两条径迹在衰变链上无关（不同 B，或含背景） | B 的径迹 × 碎裂径迹 |
| 1 姐妹 | 共享**直接**母粒子 | B_s→J/ψφ 中 J/ψ→μ⁺μ⁻ 的 μ⁺μ⁻ |
| 2 同一 B | 属于同一 B 强子但非直接姐妹 | 同一事件中 μ⁺（来自 J/ψ）与 K⁺（来自 φ） |
| 3 不同 B | 分别来自事件中两个不同的 B 强子 | B₁ 的径迹 × B₂ 的径迹 |

**这正是模型的核心输出**：对每条径迹对，预测它属于 0/1/2/3 哪一类。预测对了，衰变树就能重建；预测错了（比如把"姐妹"判成"背景"），整条链就散了。

### 1.3 为什么用 GNN 而不是传统方法

传统方法靠人工规则（空间距离、顶点拟合）逐步筛选径迹对，是一连串"先验决策"，容易把真实的关联径迹误删。GNN 的优势是：**在图上做端到端学习**——不预先假设哪些径迹对相关，让模型从数据中学会。关于"图"的概念见 §3。

---

## 2. 数据与 truth：监督信号从哪来

### 2.1 三种数据

| 数据 | 来源 | 用在哪 |
|---|---|---|
| 论文数据（Zenodo 15584745） | 组内 PYTHIA+EvtGen 模拟，含完整粒子 truth | 训练 v23、复现论文评估 |
| CERN 官方 MC（MC_normed） | 官方 LHCb 模拟 | 训练 v25、对比泛化 |
| 真实/转换数据 | 论文数据转换（训练图格式） | 训练输入 |

### 2.2 什么是 truth

**Truth = 模拟器记录的"真实答案"**。MC 模拟知道每个粒子的 PDG 编号、母粒子是谁、衰变链有多深，这些真值不经过探测器重建误差。训练时把 truth 加工成**标签（label）**：

| 标签 | 内容 | 教模型什么 |
|---|---|---|
| 边标签 `y` | 每条径迹对的 LCAG 类（0-3） | 径迹间的亲缘关系 |
| 节点标签 `ft` | 每条径迹是 b/背景（flavor tag） | 哪些径迹来自 b 强子 |
| PV 标签 `y/filter` | 每条径迹关联哪个初级顶点 | 径迹归属 |

### 2.3 类别不平衡：必须处理的核心难点

全连接图中绝大多数径迹对是**无关联（类 0）**：约 99%。如果直接训练，模型只要"全都猜 0"就得到 99% 正确率，什么都学不到。因此需要**类别加权**：给稀有类（1/2/3）更大的损失权重，逼模型去学。**这是本项目最容易出错、也最影响结果的地方**（本仓库曾因此出过 bug，见 §8）。

> 注意：truth 也用于**评估**（重建效果对比）和可选的**预剪枝**（`graph_mode: "true"` 时训练前删掉背景径迹），但训练默认不预剪枝。

---

## 3. 图表示（topology）：事件如何变成图

### 3.1 先给不懂 GNN 的你补几个概念

| 概念 | 定义 | 在本项目里 |
|---|---|---|
| 图（graph） | 由**节点**和**边**组成的结构 | 一个事件 = 一张图 |
| 节点（node） | 图中的基本单元 | 径迹（tracks）和初级顶点（PVs） |
| 边（edge） | 连接两个节点的关系 | 径迹对、径迹-PV 连接 |
| 节点/边特征 | 每个节点/边自带的数字向量 | 动量、位置、夹角等（见 §3.2） |
| 图神经网络（GNN） | 让每个节点/边"和邻居交换信息"再更新自己表示的网络 | 模型的骨干 |
| 消息传递（message passing） | GNN 的"交换信息"动作：节点反复聚合邻居的特征 | 模型的"思考"过程 |
| 异构（heterogeneous） | 图里有**不同类型**的节点/边 | tracks 和 pvs 是两种节点 |
| embedding（嵌入） | 把原始特征压缩成的低维向量 | 模型内部 16 维表示 |
| 监督学习 | 用"输入→标准答案"配对数据训练模型 | 输入=图，答案=truth 标签 |
| MLP（多层感知机） | 最基本的"全连接小网络"：向量 → 线性变换 → 激活函数 → 再变换 | 模型里每个"零件"都是一个 MLP |
| 激活函数（如 ReLU） | 给网络加"非线性"的函数，否则多层线性 = 一层线性，白堆 | 每个 MLP 中间层之间 |
| logits（对数几率） | 模型输出层的**原始数值**，不是概率 | LCAG 的 4 个 logits |
| 参数（parameters） | 模型里所有可学习的数字（MLP 的权重） | 全模型约 83 万个 |

**直觉**：消息传递就像"每一条径迹反复向周围径迹打听：你和我来自同一个 B 吗？"，问过几轮后，每条径迹的表示里就浓缩了整张图的信息。

### 3.2 图的构建

图在**数据预处理阶段**一次性建好（`convert_all_data.py`），不是模型内部动态建的：

- **径迹-径迹（tracks→tracks）**：**全连接**——每条径迹和所有其他径迹连一条边。物理上任何径迹对都有可能相关，全连接让模型自己决定谁跟谁相关。每条边带 4 个物理特征（径迹夹角、横向距离、纵向距离、是否同 PV），这些是模型判断"是否相关"的线索。
- **径迹-PV（tracks→pvs）**：每条径迹连到它所属的初级顶点。**注意**：本仓库所用论文数据的 track-PV 是**单候选**（每条径迹恰好关联一个 PV），并非全连接。
- **双向化**：把每条边复制一份反向的。这是 GNN 的技术要求（信息要双向流动），不是物理操作。

### 3.3 节点/边特征的作用（不罗列，讲作用）

| 特征 | 一句话作用 |
|---|---|
| 径迹特征（10 维） | 每条径迹的运动学（动量、原点、关联 PV 位置、电荷）——模型靠它区分"来自 B 强子的径迹"和"背景径迹" |
| 边特征（4 维） | 两条径迹的几何关系（方向夹角、空间距离）——模型靠它判断"这两条径迹是否来自同一衰变" |
| PV 特征（3 维） | 顶点坐标 |
| 全局特征（1 维） | 事件内 PV 数量，给模型一个"事件规模"的提示 |

> 一个易误解点：全连接边看似"没信息量"，但真正起作用的是**边特征**——模型不是只看"有没有边"，而是读边上的物理量来判断亲缘关系。

---

## 4. 模型：一个多任务 GNN

### 4.1 先搞清楚：模型里到底有几个"网络"

DFEI 模型看起来复杂（编码器、4 个 GN Block、解码器、各种头），但本质是**一个模型、一批参数**，由三类组件拼成：

1. **若干 MLP（多层感知机）**——就是"小全连接网络"，负责把向量变形成目标维度。模型里有几十个 MLP（每个节点类型、边类型、每个 Block 各有一个），它们**不是独立训练的模型**，而是同一个大模型里的"零件"。
2. **消息传递层（GN Block）**——图卷积的核心。
3. **推理头（inference heads）**——挂在某些位置的小 MLP，负责把特征变成"预测得分"。

可以这样理解：**整个模型是一台流水线，输入一张图，流水线里每个工位（MLP/Block）加工特征，最终产出每条径迹对的 4 类得分**。所有工位的参数一起更新（§4.4 讲怎么更新）。

### 4.2 一条数据在模型里怎么流动（层与层怎么连）

以单个事件为例，数据流是**串行**的，每一层的输出是下一层的输入：

```
事件图（原始特征：径迹10维、边4维、PV 3维、全局1维）
   │
   ▼ ① 编码器：4 组独立 MLP（径迹/边/PV/全局各一组）
       每组把特征压成 16 维嵌入（embedding）
   │
   ▼ ② GN Block #0 ──► Block #1 ──► Block #2 ──► Block #3   （串行堆叠）
       每个 Block 内部再分 3 步（见 §4.3）
   │
   ▼ ③ 解码器：又一组 MLP，把特征整理回 16 维
   │
   ▼ ④ 输出层：对每条径迹对（边）做 Linear(16→4)，得到 4 个 logits
   │
   ▼ 输出：E 条边 × 4 类得分  （这就是 LCAG 分类的"预测"）
```

**层与层之间连的是什么？** 连接的是"更新后的图"本身：Block #0 输出的（新边特征、新节点特征、新全局特征）作为 Block #1 的输入。也就是说，消息传递是"一层一层加深"的——第 1 个 Block 之后每条径迹已经"打听"过邻居，第 4 个 Block 之后它"打听"了 4 轮，特征里包含的信息越来越多。

**残差连接**：Block 的输入还会额外拼上编码器的初始嵌入（维度翻倍：16→32→48…），相当于"每层的输入都带着最初的记忆"，防止层数深了信息被稀释。

**多个事件一起训练时怎么处理（图批次）**：训练时一次通常喂多个事件（batch）。PyTorch Geometric 会把多个事件图**拼成一张大图**（节点/边按顺序接在一起），再用一个 `batch` 索引向量记录"每个节点属于哪个事件"。模型用这个索引来：
- 消息传递只在同一事件内部流动（不同事件的节点不会互相"打听"）；
- 全局特征按事件分别聚合；
- 损失按事件分别算再求平均。
这样既能利用 GPU 并行，又不会跨事件串信息。

### 4.3 一个 GN Block 内部的三步

每个 Block 内部严格按 边→节点→全局 的顺序更新：

```
Block 内部：
 ① Edge Block：对每条边，把"边特征 + 源节点特征 + 目标节点特征 + 全局特征"拼起来
                 → 过一个 MLP → 得到更新后的边特征
   └ Edge 推理头：边特征 → 小 MLP → 1 个数 → sigmoid → edge_weight（该边"是否相关"的置信度）
 ② Node Block：对每个节点，把连接它的所有边特征"加权求和"（scatter_add）
                 → 拼上节点自身特征和全局 → MLP → 更新后的节点特征
   └ Node 推理头：节点特征 → 小 MLP → 1 个数 → sigmoid → node_weight（该径迹"是否信号"的置信度）
 ③ Global Block：把整个图的边、节点特征求和 → 拼上旧全局 → MLP → 更新全局特征
```

**"加权求和"（消息传递的具体动作）**：节点 i 的新信息 = Σ(邻居边特征 × 该边置信度)。置信度高（更相关）的邻居贡献更大。这就是"径迹互相打听"的数学实现。

### 4.4 推理头、MLP 与多任务：怎么"同时训练多个 MLP"

模型里有三类推理头，各自输出不同任务的预测：

| 头 | 输出 | 对应任务 |
|---|---|---|
| 边上的 LCAG 头（输出层） | 每条边 4 类得分 | LCA 分类 |
| 每 Block 的 node 推理头 | 每个节点 1 个置信度 | 节点剪枝 |
| 每 Block 的 edge 推理头 | 每条边 1 个置信度 | 边剪枝 |
| track-PV 边上的头 | 每条 PV 边置信度 | PV 关联 |

**它们共享主干、各有独立参数**：所有头读的是同一个主干（GN Blocks）算出来的特征，但每个头自己的 MLP 参数是独立的。训练时（§4.5），总损失 = 各任务损失之和，反向传播时：

- 每个头的损失 → 梯度只更新**自己的 MLP** 和共享主干
- 共享主干同时收到所有头的梯度 → 主干学会的特征对四个任务都有用

这就是**多任务学习**：四个任务互相"帮忙"，一个任务学到的好特征能帮助其他任务。

### 4.5 GNN 到底是怎么被训练出来的

训练 = 反复执行"**前向 → 算损失 → 反向传播 → 更新参数**"直到损失不再下降。逐条解释：

**① 前向（forward）**：把一批事件图喂进模型（§4.2 的流水线），得到预测得分。

**② 算损失（loss）**：拿预测得分和 truth 标签算"差多远"。这里的关键是——**模型输出的从来不是离散答案，而是连续得分**，所以"差距"可以用一个连续、可微的数值来衡量（§5.1 细讲）。损失越小，说明预测越接近 truth。

**③ 反向传播（backward）**：数学上，把"损失对每个参数求导"。直观说：计算每个参数"该往哪个方向调、调多少，才能让损失变小"。因为整个流水线（从输入到损失）是连续可微的，这个导数可以逐层传回去（链式法则）——这就是"反向传播"。**注意：推理时用的 argmax/阈值等离散决策，在训练时不参与，所以不会卡住梯度。**

**④ 更新参数（optimizer.step）**：用 Adam 优化器按梯度调整所有参数（学习率 1e-3）。然后进入下一轮。

**几个训练术语**：

| 术语 | 含义 |
|---|---|
| batch | 一次前向处理的一组事件（本项目 batch=1，通过梯度累积 16 步等效 16） |
| epoch | 完整看一遍训练集（40K 事件约 1 小时） |
| 验证集 | 训练时留出的"模拟考"，每 epoch 算一次验证损失 |
| 学习率 | 每次更新参数的步长；验证损失停滞就减半（ReduceLROnPlateau） |
| early stopping | 验证损失 15 个 epoch 无改善就提前停止 |
| checkpoint | 定期保存模型参数；"best" 是验证损失最低的那个 |

### 4.6 PID 信息（可选）

`use_pid` 配置决定是否把粒子种类（PID）拼进输入特征：`"None"`（不用）/ `"true"`（用真值 PID）/ `"realistic"`（用重建 PID）。论文数据无 PID，因此 v23 用 `"None"`。

---

## 5. 监督学习：truth 怎么变成模型的"考试标准"

### 5.1 关键疑问：标签是离散的，损失怎么算？

LCAG 标签是 0/1/2/3，剪枝标签是 0/1——都是**离散**的。而训练要算梯度，梯度只对**连续函数**有意义。怎么绕过去的？

**答案：模型不输出离散答案，输出"连续得分"（logits），损失定义在连续得分上。**

**① 多分类（LCAG）用 logits + softmax + 交叉熵**

- 对每条边，模型输出 4 个数字（logits，可以是任意实数，如 [0.2, 1.8, -0.5, 3.1]）。
- **softmax** 把这 4 个数字变成 4 个和为 1 的正数（概率分布）：数值大的 logit 对应概率大。比如 [0.2, 1.8, -0.5, 3.1] → [4%, 26%, 3%, 67%]，即模型认为这条边有 67% 是类 3。
- **交叉熵（CrossEntropy）** 衡量"这个概率分布离真值（比如真值是类 1，即 one-hot [0,1,0,0]）有多远"：`loss = -log(模型给真值类的概率)`。真值类被给的概率越大，损失越小。
- **梯度**：交叉熵对 logits 的导数恰好是 `(softmax 输出) − (one-hot 真值)`——一个简单、连续的向量。模型会把 logits 朝"真值类更大"的方向推。这是光滑的连续优化，不是离散跳变。

**② 二分类（剪枝/PV）用 logits + sigmoid + 二元交叉熵**

- 模型对每个节点/边输出 1 个 logit。
- **sigmoid** 把它压到 (0,1)（σ(z)=1/(1+e⁻ᶻ)），即"是信号/相关的概率"。
- **二元交叉熵（BCE）**：`loss = −[y·log(p) + (1−y)·log(1−p)]`，其中 y∈{0,1} 是真值，p 是 sigmoid 输出。真值 y=1 时，p 越接近 1 损失越小；y=0 时反之。
- **梯度**：BCE 对 logit 的导数 = `sigmoid 输出 − 真值`，同样是连续光滑的。

**一句话**：离散标签只出现在损失公式里的"常数"一侧，模型输出的连续得分才是变量，所以整个训练过程可微、可反向传播。

### 5.2 四个任务的损失（具体怎么算）

总损失 = 四个任务损失之和，每个任务在各相关位置取标签和预测：

```
总损失 = LCA损失 + 节点剪枝损失 + 33×边剪枝损失 + PV关联损失
```

| 任务 | 预测来自 | 真值标签 | 损失公式 |
|---|---|---|---|
| LCA 分类 | 输出层每条边的 4 logits | 边标签 y∈{0,1,2,3} | CrossEntropy(softmax(logits), y) |
| 节点剪枝 | 每个 Block 的 node 推理头 logit | ft≠1（信号/背景） | BCEWithLogits(logit, y) |
| 边剪枝 | 每个 Block 的 edge 推理头 logit | y>0（相关/无关） | BCEWithLogits(logit, y) × 33 |
| PV 关联 | track-PV 边 logit（仅 filter=1 的径迹） | 是否正确关联 | BCEWithLogits(logit, y) |

**三个重要细节**：

1. **33× 是什么**：边剪枝任务里"无关边"占压倒性多数（§2.3），单独放大它的损失，让这个任务在总损失里不至于被忽略。
2. **多 Block 都算损失**：节点/边/PV 的损失在**每个 GN Block** 的推理头上都算一遍再求和（`for block in blocks: loss += ...`）。这样每个中间层都"有老师教"，而不是只教最后一个输出层。
3. **PV 只对有意义的径迹算**：用 `filter` 掩码排除 ghost 径迹后再算损失。

### 5.3 多个 MLP 怎么同时被训练

模型里有几十个 MLP（各编码器、各 Block 的边/节点/全局、各推理头），它们"同时训练"的机制是：

1. **前向**：所有 MLP 串行/并行地在前向里被调用一次，各自算出自己的输出。
2. **总损失**：四个任务的损失求和，得到一个数。
3. **反向传播**：这个数对**所有参数**（包括每个 MLP 的权重）求导。数学上，总损失 = L₁+L₂+L₃+L₄，那么 `∂L/∂θ = ∂L₁/∂θ + ∂L₂/∂θ + ...`，即每个任务的梯度直接相加——**每个 MLP 收到的梯度是四个任务贡献之和**（当然，那些和它无关的任务贡献为 0）。
4. **一步更新**：所有参数用各自梯度一起更新一次。

所以"同时训练多个 MLP"不是一个一个轮流训，而是**一次前向 + 一次反向 + 一次更新**把整个模型的全部参数都更新了。共享主干（GN Blocks）因为同时被四个任务监督，学到的是对四个任务都通用的特征。

### 5.4 类别权重（关键机制）

由于类别严重不平衡（§2.3），每个任务都用**逆频率权重**放大稀有类：

- 二分类（BCE）：`pos_weight = 负样本数 / 正样本数`
- 多分类（CrossEntropy）：`weight = 总样本数 / (类别数 × 该类样本数)`

权重作用在损失公式里：真值属于稀有类时，它的损失乘以大权重，产生更强的梯度，逼模型重视稀有类。**如果权重没生效（等于全 1），模型只会"全猜背景类"**——这正是本仓库曾经踩过的坑（§8.1）。

---

## 6. flag（配置开关）：控制模型"练什么、怎么练"

### 6.1 先澄清：此 flag 非彼 FLAG

你可能会想到图增强方法 **FLAG（Free Large-scale Adversarial Augmentation on Graphs）**。**本仓库程序里并没有实现或使用 FLAG**（没有图对抗增强代码）。这里讨论的 "flag" 指 **YAML 配置里的布尔开关**，控制模型训练/推理的行为。

代码里另一个带 "flag" 意味的是 `ft`（**flavor tag**，flavor tagging 标签）：0=反 b、1=背景、2=b。它是节点标签，不是数据增强。

### 6.2 配置开关分三类

**① 任务开关**（`inference:` 部分）——决定训练哪些监督任务：

```yaml
inference:
  LCA: true            # 开 LCAG 4类分类
  node_prune: true     # 开节点剪枝任务
  edge_prune: true     # 开边剪枝任务
  pv_asso: true        # 开 PV 关联任务
  edge_prune_thr: 0.2  # 推理时边剪枝阈值
  node_prune_thr: 0.2  # 推理时节点剪枝阈值
```

每个任务还可单独配 `*_weights` 开关（是否用类别权重）。**这些开关的意义**：可以逐步验证（先只开 LCA 跑通，再开其他任务），也可以控制资源占用。

**② 数据处理开关**（`settings:` 部分）：

| 开关 | 作用 |
|---|---|
| `graph_mode` | `"None"`=不预剪枝（训练默认）；`"true"`=训练前用 truth 删背景径迹 |
| `pv_model` | 是否/如何做 PV 关联预处理 |
| `precision` | 数值精度：本项目必须用 FP32（FP16/bf16 会出 NaN，见 §8.3） |
| `use_pid` | 是否拼接 PID 特征（§4.3） |

**③ 评估绘图开关**（`plt_nodes/plt_edges/plt_pvs`）——是否在测试时输出 ROC 曲线等诊断图。

**一句话总结 flag 的作用**：flag 决定"模型练哪几个任务、用什么阈值剪枝、预处理怎么走"——它是模型的"控制面板"，让同一套模型代码能配置出不同的训练方案。

---

## 7. 从预测到物理结果：重建与评估

### 7.1 重建

模型输出的 LCAG 预测（每条径迹对属于 0-3）经"剪枝→按 LCA 关系聚类"即可拼出衰变树：姐妹边（类 1）把径迹连到同一母粒子，同一 B 的边（类 2）把子树拼成整棵 B 衰变树。程序在 `reconstruction.py` 实现，用真值 LCA 与重建 LCA 对比打分。

### 7.2 评估指标（论文口径）

对每条真值链，按重建结果归类：

| 指标 | 含义 | 论文（WHGNN） |
|---|---|---|
| PerfectReco | 整条链的径迹和 LCA 关系**全部**重建正确 | 21.5% |
| AllParticles | 径迹集合对但 LCA 关系不完全对 | 19.3% |
| NoneIso | 链上混进了背景径迹 | 45.8% |
| PartReco | 只重建出一部分 | 13.5% |

评估必须有**粒子级 truth**（`final_keys`、`truth_senders` 等），否则只能近似评估。本仓库曾因转换脚本丢了这些字段而一度无法做论文口径评估（§8.2）。

### 7.3 当前结果速览（截至 2026-08）

| 模型 | 数据 | 训练 | PerfectReco |
|---|---|---|---|
| v25 | CERN 官方 MC | 100 epoch 完整 | **27.7%**（口径与论文不同，仅参考） |
| v23 | 论文数据 | 75/100 中断 | **16.5%**（单 B 事件 21.05%） |
| v23（重训中） | 论文数据 + 权重修复 | 进行中 | 待定 |

---

## 8. 本仓库踩过的坑（与代码强相关）

### 8.1 类别权重 bug（最关键）

`weights_calculator.py` 的 `transform_pos_weight` 检查键名 `LCA__weights`（双下划线），而配置文件里是 `LCA_weights`（单下划线）→ 条件永远不成立 → **所有类别权重静默保持 1.0**，类别不平衡从未补偿。后果：模型只学会"背景 vs 非背景"，LCAG 类 1（姐妹）准确率 = 0%，重建性能大打折扣。

- 来源：Yukai Zhao 在 `yukai_IFT` 分支重构权重计算时引入；他后来在 `yukai_da_test` 分支修复（commit `d9d38af`），但**未合入 master、也未同步到 `yukai_IFT`**。
- 现状：已在本工作分支修复并**重新训练 v23/v25**。
- 教训：改这类"从配置键名读取"的代码后，务必验证 `pos_weight` 实际值不是全 1。

### 8.2 论文数据转换丢失 truth

`convert_all_data.py` 原版只保留训练字段，丢弃了评估必需的粒子级 truth（`final_keys`、`truth_senders`、`truth_moth_ids`、`lca_chain`、`truth_part_keys` 等）→ 论文口径评估跑不了。已修复并重新转换（`converted_LHCb_truth`）。

### 8.3 数值精度

FP16/bf16 训练在本项目数据上会产生 NaN（v22 因此失败），**必须用 FP32**。

### 8.4 推理时的 pv_filter 崩溃

`reconstruction.py` 假设 track-PV 图稠密（每条径迹连所有 PV），在论文数据（单候选）上直接崩溃。已改为按径迹分组聚合，兼容两种图。

### 8.5 续训的坑

- **LR 重置**：续训加载 checkpoint 时若用 config 的初始 LR（1e-3），会覆盖 checkpoint 里已衰减的 LR（6.25e-5），导致已收敛模型发散。续训应把 `lr` 设为与 checkpoint 状态匹配的小值。
- **内存**：训练作业若内存接近提交上限（32GB），会被 HTCondor hold 并反复重跑丢进度。提交时用 `-m 64000`。
- **坏 GPU**：`gpu02.farm.lzu.edu.cn` 上存在损坏 GPU 仍被调度，提交脚本已加预检（快速失败）＋自动重提。

### 8.6 剪枝 vs 非贪心重建

推理剪枝阈值越高，被剪掉的真值正边和完整链越多（分析数据：阈值 0.2 时约丢 18% 完整链，0.5 时约丢 36%）。如果希望"不贪心"，可考虑不做硬剪枝、用全部 LCAG 预测＋软权重走重建（尚未实现，是待验证方向）。

---

## 9. 快速复现指引

```bash
# 环境：conda env dfei，PyTorch 2.6 + PyG 2.8 + Lightning 2.6（详见 requirements.txt）

# 训练（提交 GPU 作业，论文数据 v23 重训）
hep_sub submit_train_public.sh -g ghigh -gpu 1 -cpu 8 -m 64000 -wt long \
  -o logs/train_public_fix.out -e logs/train_public_fix.err

# 评估（论文口径，含粒子 truth 的转换数据）
hep_sub submit_eval.sh -argu eval_v23_full.yaml -g ghigh -gpu 1 -cpu 4 -m 32000 -wt mid \
  -o logs/eval.out -e logs/eval.err
```

输出位置：`LHCb_logs/DFEI/version_X/`（checkpoints、metrics.csv、评估 CSV 与 info 文本）。

---

## 10. 关键文件地图

| 文件 | 作用 |
|---|---|
| `wmpgnn/model/model.py` | DFEI_HGNN 模型定义 |
| `wmpgnn/lightning_module/dfei_lightning_module.py` | 训练/测试循环、多任务损失、评估输出 |
| `wmpgnn/data_loader/chunk_loader.py`、`helper.py` | 数据加载、双向化、预剪枝 |
| `wmpgnn/data_loader/weights_calculator.py` | 类别权重（§8.1 bug 所在） |
| `wmpgnn/reconstruction/reconstruction.py`、`reco_helper.py` | LCAG 重建与评估 |
| `convert_all_data.py` | npy→图数据转换（含 truth 保留） |
| `config_files/*.yaml` | 全部 flag/超参数配置 |
| `submit_*.sh` | HTCondor 提交脚本 |

> 本页面向不熟悉 GNN 的读者，用"物理问题 → 图 → 模型 → 监督 → 评估"一条主线讲述。详细特征清单、论文指标推导等可查 `Dataset.md`、`TRAINING_PROGRESS_REPORT.md` 与论文原文。
