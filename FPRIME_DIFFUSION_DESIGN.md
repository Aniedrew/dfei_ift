# 方案 F'：扩散分数 + Sweep Cut 技术原理（零基础版）

> 更新：2026-08-14
> 定位：方案 F（seed-expand 贪心 top-k）的升级版。这份文档**不假设任何机器学习背景**，用生活比喻讲清每一种技术。
> 代码：`wmpgnn/reconstruction/topk_selection.py`（`ppr_diffuse` / `heat_kernel_diffuse` / `sweep_cut` / `node_expand_diffuse`）

---

## 0. 30 秒结论

我们想解决一个问题：**怎么从一堆粒子中，把"属于同一次衰变"的那几个粒子尽可能完整地找出来**。

- **方案 F**（上一版）的做法像"贪心挖矿"：从最可信的粒子出发，每次只挑最可信的 k 条连接往前挖，走几步就停。
- **方案 F'**（本版）的做法像"往池塘里倒墨水"：从最可信的粒子（墨水点）出发，让墨水沿着连接自然扩散，最后**看哪里被染得深**，把染色深的区域整个捞起来。

墨水扩散能"看到全局"，贪心挖矿只能"看到眼前一步"——这就是 F' 的核心优势。

---

## 1. 先建立共同语言：什么是"图"（Graph）

物理事件可以被画成一张**图**：

```
        粒子 A ──(关联)── 粒子 B
          │                  │
          │                  │
        粒子 C ──(关联)── 粒子 D
```

- **点（节点）**：一个粒子（径迹）。
- **线（边）**：两个粒子之间"可能有关联"（比如可能是同一个 B 介子的衰变产物）。
- **每条线都有一个"可信度"**：0 到 1 之间的数字，1 = 几乎肯定有关联，0 = 几乎肯定没有。

一个典型事件：约 150 个点、几万条线（大部分线的可信度都很低，是背景噪声）。

我们最终希望：把这张图**切分成几堆**，每一堆正好对应一条真实的衰变链。

---

## 2. 为什么要升级？方案 F 的"近视"问题

方案 F 的算法是：

```
1. 先保留可信度高的点（种子）
2. 每个种子点，看它连着的线里可信度最高的 k 条，保留
3. 如果这些线连到了被删掉的点，把那个点"救回来"
4. 对新救回来的点，重复第 2 步，但 k 减 1
5. 直到 k 减到 0
```

**它的问题**：每一步只看"眼前的 k 条线"（局部决策）。打个比方：

> 你在黑夜里找人。手电筒只能照到面前几米。你顺着一条路走，结果这条路在两步前就拐弯了，你根本看不见拐弯后的路是不是通向要找的人。

具体会出两种错：
1. **漏**：链里某条线可信度低，但它是**连接整条链的关键桥**。贪心 top-k 看不到"过了这座桥后面还有一大群人"，就把桥丢了 → 整条链断。
2. **误**：某个高分背景线把不相关的点连了进来，贪心没能力发现"这个点是孤立的、周围没有同伴" → 背景混入。

F' 想用"全局视野"来解决这两个问题。

---

## 3. 核心技术一：图扩散 / 随机游走（Graph Diffusion / Random Walk）

### 3.1 生活比喻：传染病

想象一个村庄（点），村民之间会串门（边）。现在一个村民得了传染病（种子）：

- 他传染给串门的邻居；
- 邻居再传染给他们的邻居；
- ……病毒沿着"串门关系"扩散。

**谁离病人越近、谁被串门越多，谁染病的概率越高。** 染病概率 = "扩散分数"。

在图上，我们把"传染概率"换成"**从种子出发、沿边游走的概率**"，这就是**随机游走**：

```
第 0 步：你站在种子点上，概率 = 100%
每走一步：你从当前点，沿着边，随机走到一个邻居
```

走很多步之后，**你停留在某个点的概率**就是那个点的"扩散分数"。分数高的点 = 与种子连通性强 = 大概率属于同一条链。

### 3.2 为什么边可信度参与？

真实图里边的可信度不同。我们让"可信度高的边更容易被走到"（相当于：病毒更容易沿"亲密关系"传播）。

> 病毒从 A 出发：A 有两条边，一条可信度 0.9（疑似真关联），一条 0.1（疑似噪声）。那 90% 的概率病毒走那条高可信的边。低可信边几乎传不过去。

数学上就是把"边可信度"当作"随机游走的权重"。走完很多步，**高分节点的"热度"会集中在与种子真正连通的区域**，噪声被自然稀释。

### 3.3 实现（一句话）

- 构造一个"转移表"：`从点 i 走到点 j 的概率 = 边(i,j)可信度 / i 的所有边可信度之和`。
- 从种子出发反复乘这个表（迭代几十次），得到每个点的扩散分数。

> 这就是 `ppr_diffuse` / `heat_kernel_diffuse` 做的事，只是它们用两种不同的"游走规则"（见第 4、5 节）。

---

## 4. 核心技术二：个性化 PageRank（PPR）

### 4.1 来源：网页排名

PageRank 是 Google 最初的网页排名算法："被重要网页链接的网页更重要"。它在图上做**随机游走**，但加了一条规则：

> 游走者每次有 **α 的概率沿边继续走**，同时有 **(1-α) 的概率"瞬移"回起点**。

### 4.2 "个性化"是什么意思

普通 PageRank 的起点是"所有网页均等"。**个性化 PageRank** 把起点固定为**我们指定的种子集合**：

```
p = (1-α) × 种子分布 + α × (沿边走一步后的分布)
```

迭代到最后，`p[i]` = "一个游走者，总是忍不住瞬移回种子，其余时间在图上乱走，最终停留在点 i 的概率"。

### 4.3 生活比喻：遛狗

> 你（种子）出门遛狗。狗有 α=85% 的概率跟着你往前走（沿边游走），有 15% 的概率挣脱绳子跑回你脚边（瞬移回种子）。走很久之后统计：狗在哪个位置停留的时间最长？——停留时间 = PPR 分数。
>
> 离你越近、连通越强的区域，狗待的时间越长。

### 4.4 参数 α 的含义

| α 值 | 含义 | 效果 |
|---|---|---|
| 0.85（默认） | 狗 85% 时间向前走 | 中等扩散，能覆盖 2-3 跳的邻居 |
| 接近 1（如 0.95） | 狗几乎不回来 | 扩散很远，把大块区域都染上 |
| 较小（如 0.5） | 狗老被拽回来 | 只染种子附近，扩散范围小 |

**在 DFEI 里**：α=0.85 意味着"从种子出发能看到 3 跳左右的邻居"——正好覆盖一条典型 B 衰变链（3-8 个粒子）的长度。

---

## 5. 核心技术三：Heat Kernel（热传导）

### 5.1 生活比喻：往凉水池里放一根热铁棒

> 把一根烧红的铁棒（种子）放进一池凉水（图）。热量沿水传导：
> - 离铁棒近的水，先热起来（分数高）；
> - 热量随时间继续扩散，远一点的水也慢慢热；
> - 池子角落的水，可能一直不热（与铁棒几乎不连通）。

**温度 = Heat Kernel 扩散分数。** 时间 `t` 是唯一的参数：`t` 越大，热量扩散得越远（能看到越远的连通性）。

### 5.2 与 PPR 的区别

| | PPR | Heat Kernel |
|---|---|---|
| 机制 | 随机游走 + 瞬移回种子 | 热量沿边扩散（像偏微分方程） |
| 参数 | α（瞬移概率） | t（扩散时间） |
| 扩散形态 | 在种子附近"徘徊"，衰减较缓 | 以种子为中心"均匀向外晕开"，衰减平滑 |
| 谁更合适 | 想要"以种子为核心"的强连通区域 | 想要"平滑的连通强度场" |

实证研究（Kloster & Gleich 2014）表明 Heat Kernel 在局部社区发现上通常优于 PPR，因为它对"扩散多远"的控制更平滑。

---

## 6. 核心技术四：Sweep Cut（扫描切割）——怎么把"分数"变成"取舍"

扩散之后，每个点有了分数。问题是：**分数多高才算"保留"？**

直接设阈值（比如分数 > 0.1 就留）的问题：阈值对不同事件不通用（有的事件分数整体高，有的整体低）。

**Sweep Cut 的做法**（更聪明）：

### 6.1 生活比喻：切蛋糕要切在最"自然"的地方

> 有一根腊肠（节点按分数从高到低排好队）。你拿刀从左往右比划，看每一刀下去"切面是不是最干净"——即左侧区域内部紧密、与右侧几乎不粘连。
> 找到那个"切面最干净"的位置，就切在那里。

### 6.2 数学定义：conductance（传导率 / 干净度）

对任何一"堆"点 S（前 k 个高分点），定义它的"干净度"：

```
conductance(S) = 从 S 连到外面的线的总可信度
                 ──────────────────────────────
                 min(S 内部的总线权重, 外面的总线权重)
```

- 分子 = "泄漏"：S 与外界的连接强度（越小越好，说明 S 自成一体）
- 分母 = "规模"：S 的大小（防止"只留 1 个点"这种作弊——1 个点当然没有外连，但它没有意义）

**conductance 越小 = 这一堆点越是"浑然一体的团块"。**

### 6.3 算法

```
1. 把点按扩散分数从高到低排序
2. 逐个把点加入集合 S（前 1 个、前 2 个、前 3 个……）
3. 每加一个点，算一次 conductance
4. 取 conductance 最小的那个前缀作为保留集
5. 分数低于 "最高分 × 5%" 的点直接不允许加入（防噪声尾巴）
```

### 6.4 为什么这解决了方案 F 的两个问题

- **漏**：扩散分数让"链里的桥"有了较高的分（因为它连着整个链，游走者经常路过），sweep cut 会把桥连同桥后面的链一起保留 → 链不再断。
- **误**：孤立的高分背景点，扩散分数会很低（周围没有同伴，游走者无法停留），排在队尾，不会被纳入保留集 → 背景不混入。

---

## 7. 整合：F' 的完整流程

```
输入: 每个点的可信度, 每条边的可信度
│
├─ ① 种子: 可信度 > 阈值的点 (或: 可信度最高的前 N% 个点)
│
├─ ② 扩散: 从种子出发, 用 PPR 或 Heat Kernel 算出每个点的"连通强度"分数
│     (这一部看的是"全局连通", 不是局部 k 条边)
│
├─ ③ Sweep Cut: 按分数排序, 找 conductance 最小的前缀 → 决定保留哪些点
│
├─ ④ 边: 保留点集合内部的边, 再对每个点做 top-k 收缩 (限制边的数量)
│
└─ 输出: 保留下来的点和边 → 送入衰变链重建
```

代码入口：`node_expand_diffuse(node_weight, edge_weight, edge_index, ...)`。

---

## 8. 参数速查表（配置 `inference` 段）

| 配置键 | 默认 | 含义 | 调参方向 |
|---|---|---|---|
| `node_expand` | false | 总开关（true 启用方案 F/F'） | — |
| `node_expand_method` | "topk" | `"topk"`=方案F；`"ppr"`/`"heat"`=方案F' | 先跑 F，再试 ppr/heat |
| `node_expand_seed_thr` | 0.5 | 种子阈值 | 越大种子越少，越依赖扩散 |
| `node_expand_ppr_alpha` | 0.85 | PPR 瞬移概率 | 越小扩散越近；0.9 看更远 |
| `node_expand_heat_t` | 3.0 | Heat Kernel 扩散时间 | 越大扩散越远 |
| `node_expand_min_score_frac` | 0.05 | sweep cut 最低分数线（相对最高分） | 越大保留越少 |
| `node_expand_k0` | 12 | 边收缩的 per-node top-k | 限制候选边数量 |
| `node_expand_edge_thr` | 0.0 | 边最低可信度（0=全保留） | NoneIso 高时上调 |

**建议实验顺序**：`F(topk)` 出基线 → `F'(ppr)` → `F'(heat, t=3)` → `F'(heat, t=8)`，对比 PerfectReco 与 NoneIso。

---

## 9. 这些技术的学术出处（想深入再读）

| 论文 | 贡献 | 一句话 |
|---|---|---|
| Spielman & Teng, 2004 | Nibble 局部图分割 | 从种子出发、只花"输出簇大小"的时间找好簇（F' 的源头） |
| Andersen, Chung & Lang, FOCS 2006 | **PageRank-Nibble** | 用个性化 PageRank + sweep cut 做局部聚类，有理论保证 |
| Kloster & Gleich, 2014 | HeatKernel-PageRank | 用 heat kernel 扩散替代 PPR，实证效果更好 |
| Jeh & Widom, 2003 | Personalized PageRank 基础 | "个性化"随机游走的原始定义 |

---

## 10. 为什么这对 DFEI 有意义（回到物理）

DFEI 重建的终极目标：**把每条 B 介子衰变链完整找出来，且不混入无关粒子**。

- 方案 F 保证"链上只要有 1 个粒子被选中，整条链都找得回来"（连通性恢复）；
- 方案 F' 在此基础上，用"扩散 + 干净切割"**避免把无关粒子也拉进来**——这正是 NoneIso（混入背景）的直接对策。

两者配合训练侧的 loss 再平衡（`OPTIMIZATION_REPORT.md` §4.4），形成"推理侧恢复连通 + 训练侧强化分类"的完整闭环。
