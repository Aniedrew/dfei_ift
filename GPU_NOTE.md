# LZU 服务器 GPU 情况研究笔记

> 创建：2026-08-17（基于 7-31 至 8-17 期间作业日志的 GPU UUID / 型号 / 速度还原）
> 用途：后续分析训练效率（epoch 时间、卡死、CUDA 错误、时快时慢）时纳入考虑
> ⚠️ 原则：不做抢算力/挑卡调度，仅记录事实供分析使用

---

## 1. GPU 池构成（作业日志还原）

| 节点 | 显卡 | 显存 | 已见 UUID | 实测训练速度 (epoch ~31k steps) |
|---|---|---|---|---|
| **gpu10** | **NVIDIA L20**（专业卡） | 48 GB | 4c3ebb40 | **16.95 it/s，31:33/epoch** |
| gpu06 | RTX 3080（30系） | 10 GB | 50e21a39, ba6b369d, da762256 | 11.98 it/s，44:54/epoch |
| gpu08 | RTX 3080 | 10 GB | a9c95f1c, 8e0233b5 | ~11.98 it/s（推测同 3080） |
| **gpu02** | **RTX 2080 Ti**（20系） | 11 GB | 4cf8104e, a2db080d, e2ac1338, +1 | 9.75–11.78 it/s，45–52 min/epoch |

- GPU UUID 与物理卡一一对应；同一 UUID 出现在不同作业日志 = 同一张卡被多次/并发使用。
- L20 比 2080 Ti 快 ~44%（16.95 vs 9.75 it/s），2080 Ti vs 3080 差距很小（~10%）。

## 2. 关键现象：为什么"时快时慢"

### 2.1 主因：调度器把多个 GPU 作业并发塞到同一张物理卡（CUDA 分时共享）

- 铁证：`train_public_resume2`（08-09 15:16 启动）与 `train_public_resume3`（08-09 15:18 启动）间隔 2 分钟，**落在同一张卡**（UUID 4cf8104e，gpu02 2080Ti）。
- 两张卡并发跑，各自算力被瓜分 → 训练速度断崖下降。
- 实例：`eval_v31_f_thr09`（3080 gpu06）20 个测试 chunk 从 11:38 跑到 19:19（约 8 小时，每 chunk 间隔 44 min）；同卡同口径基线 `eval_v31_base_thr09` 仅 18 分钟。除方案 F 本身重建复杂度高外，GPU 被并发抢占是重要因素。

### 2.2 次要因素

- **CPU/IO 瓶颈**：chunk 加载（zstd 解压 + 图构建）耗时与 GPU 无关；队列拥挤时文件 IO/CPU 变慢，会拉长每 chunk 间隔（日志 `[chunk] ... done chunk N` 的时间差包含加载+前向+重建全过程）。
- **型号差异真实存在但非决定因素**：L20 vs 2080Ti 差 ~44%，但 2080Ti vs 3080 差 ~10%。
- **评估阶段的 it/s 不能与训练混淆**：训练日志中曾出现 `Testing ... 0.48it/s`，这是评估（重建后处理）速度，不代表训练卡顿。

## 3. 与历史问题（"20系出问题"）的关系

- gpu02 的 2080 Ti 被**反复高强度使用**（本组 12+ 个作业 + 其他用户），加上可能的并发共享——"坏卡"更可能是**过载/资源竞争**而非硬件损坏。
- 观察到的 `RuntimeError: CUDA driver error: unknown error` / `CUDA-capable device(s) is/are busy or unavailable` 常见于：
  - GPU 被其他作业抢占后显存状态错乱；
  - 同一物理卡同时跑多个作业时 CUDA context 冲突；
  - 长时间运行后驱动状态异常。

## 4. 对训练效率分析的影响（后续分析时纳入）

1. **比较 epoch 时间前必须记录 GPU UUID/型号**，否则 45min 与 31min 的差异会被误归因于模型改动而非硬件/竞争。
2. **"卡死/变慢"排查顺序**：
   1. 该作业 GPU UUID 是否与另一个运行中作业重叠（并发共享）；
   2. chunk 加载间隔是否异常（IO/CPU）；
   3. 是否落在 2080Ti（相对慢 44%）；
   4. 才考虑模型/代码问题。
3. **作业日志的 `[PREFLIGHT]` 已打印 GPU 型号**（如 `NVIDIA GeForce RTX 3080`），可在分析时直接 grep 定位。
4. **评估耗时对比需同卡同口径**：基线/F/B2 评估若分到不同卡，耗时与指标差异需剔除硬件因素。

## 5. 已记录的事实速查表（作业 → GPU）

| 作业 | 节点 | 显卡 | UUID |
|---|---|---|---|
| train_cern_new | gpu10 | L20 | 4c3ebb40 |
| train_cern_next (v36) | gpu02 | 2080Ti | 4cf8104e |
| train_cern_resume (v35) | gpu06 | 3080 | 50e21a39 |
| eval_v31_base_thr09 | gpu06 | 3080 | ba6b369d |
| eval_v31_f_thr09 | gpu06 | 3080 | da762256 |
| eval_v31_base (早期) | gpu08 | 3080 | a9c95f1c |
| eval_v31_f (早期) | gpu08 | 3080 | 8e0233b5 |
| train_cern_new_fix (v31) | gpu02 | 2080Ti | e2ac1338 |
| gpu_probe / train_public 系列 | gpu02 | 2080Ti | a2db080d / 4cf8104e / e2ac1338 |

---

> 注：本笔记仅为事实记录，未做任何抢占/挑卡/迁移作业的操作；如需复用 GPU 池信息做调度，请先与集群管理员确认策略。
